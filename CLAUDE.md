# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Karawan is a C# game engine ("Joyce") and game ("Silicon Desert 2") targeting .NET 9.0. It runs on Windows, Linux, macOS, and Android.

## Quick Start (For New Instances)

**First, read these in order:**
1. `PROCESS.md` — Generic development workflow, mandatory documentation updates
2. `PROCESS_TALE.md` — TALE-specific workflow and test commands
3. `docs/testing/docs/` — Testing infrastructure (see `TESTING_STRATEGY.md`)
4. `PROCESS_DOCS.md` — Documentation organization guidelines

**Current Status (as of September 5, 2026):**
- 📍 **Working on the three-dimensional city? Read `docs/roadmap/proposed/CITY-3D-OPEN-POINTS.md` FIRST.** It is the open ledger: what is still broken, what was deliberately deferred, and how this work has actually gone (measure before diagnosing; the obvious diagnosis was wrong in about half of these rounds). `docs/roadmap/proposed/STREETS-3D-TOPOLOGY.md` is the design and history beside it (§7a … §7s). **Part 1 of the ledger is CLEAR, including items (k), (l), (m) and (n)** — (m) was reported and fixed on 2026-09-05 and left one new open item behind it, **the road mesh does not represent its own surface**, which is (n) and was fixed the same day (§7s). One deliberate remainder besides: **what an intercity line IS** (a graded embankment, a viaduct on pylons, or a deliberately elevated line) is a design decision the owner has not made, written out with its measurements under ledger item (e) and in §7n.
- 🆕 **Grade separation exists as machinery and is OFF: `joyce.EnableGradeSeparation` (WP-B1, 2026-09-05; `docs/roadmap/proposed/STREETS-3D-PHASE-B-CROSSING-POLICY.md` §7)**. Phase B's first work package wires up `ClearanceConstraint` and `SpanLengthConstraint`, which had been **built, unit-tested and absent from `Generator._buildPipeline`**; nothing creates ramps yet, so it was latent. **The flag is injected into `Generator` like `RuleTable`, with the one global read in `ClusterDesc._generateStrokes`** — not read from `GlobalSettings` deeper in, for `StreetHeightSources.FollowsTerrain`'s recorded reason (a process global cannot be driven both ways in one test run), and a scan keeps that the only read. **Say `Ramp`/`Bridge`/`Tunnel`, never "non-`Street`"**: `ConnectorBridge` strokes exist in shipped flat cities (3/3/2/1 on seed011@500, seed017@2400, seed000@1500, Yelukhdidru@800) and are ordinary ground roads, so `StrokeKinds.IsStructure` names its three kinds explicitly and is the one expression that does. **Placement in the pipeline is behaviour and is now asserted by name**: both new constraints sit after `StrokeNearPointConstraint` — the *last* constraint that can `Restart`, which is stronger than the brief's "after `SnapToNearbyPoint`" — so a rejection is judged on the geometry the candidate finally has, and before `IntersectionConstraint`, the most expensive check. ⚠️ **THE BIGGEST FINDING: `StrokeStore.GetRampsNear` could not detect a crossing at all.** It tested the four endpoint-to-segment distances, which are the distance between two segments only when they do NOT cross — two segments crossing at their midpoints have all four endpoints far away — so it returned nothing for *a street laid straight through a ramp*, the one case the rule exists for. That was survivable while the ramp still got a `Split` verdict; **B1.4 removes the split (a structure is invisible to `IntersectsMayTouchClosest` by `Kind`), so wiring B1.4 without repairing the query would have driven a road clean through a ramp with nothing recording it** — strictly worse than the defect it fixes, and silent. It surfaced only because the test fixture measured clearance independently instead of mirroring the implementation. ⚠️ **`IntersectionConstraint` also gave every crossing junction `Level = 0`** whatever deck it was found on (zero-cost to fix: 0 is the default). ⚠️ **B1.3's level filter is needed on BOTH loops of `_bridgeOrphanToMain`**, which neither the plan nor the brief said — the first loop picks which end of the ORPHAN the bridge leaves from, so filtering only the partner choice yields a level-correct bridge from the wrong end; that mutation survived until a second fixture was built. **`SplitStrokeAt` refuses a structure, and a split point on another deck, before it removes anything from the store** — the backstop for the fact that it used to call `AddStroke` directly, bypassing `NetworkBuilder._checkLevels` entirely. **`RampClearance` is supplied only with the flag on** (`GetRampsNear` news two lists per call; supplying it unconditionally keeps every fingerprint and trips `StreetCostTests`, confirmed by mutation), and it defaults to `Stroke.WidthForWeight(weightMax)` — the widest carriageway the ruleset can build, which required hoisting `StreetWidth()`'s expression into a static so there is still one copy. **THE DEFAULT CITY DOES NOT MOVE: no baseline file differs from `origin/master` by a byte** (V1 fingerprints, `street-geometry.json`, the allocation ceiling), and V2 — which has no baseline of its own — is asserted to be *determined* by V1 on all eight seeds rather than recording a second file. **Mutation survivors: two of twenty, both provably EQUIVALENT** — routing `ConnectComponentsPass`'s two emitters through `NetworkBuilder` cannot be caught by anything, because given the level filter both ends of a connector are on the same level by construction and `_checkLevels` can never refuse a `ConnectorBridge`; it is a backstop against the filter being removed, and removing the filter is itself killed. Tests: `tests/JoyceCode.Tests/engine/streets/{GradeSeparationPipelineTests,ConnectComponentsLevelTests,DeckElevationDriftTests}.cs` (47 new; 1211 xUnit against 1164, TALE 200/200). **Found and NOT fixed at the time, two of the three since fixed**: ✅ the corridor mid point (see the next entry); ✅ `Generate()` running `_connectPass.Run()` on both exits (WP-B2, below); ⚠️ **still open**: the connect pass does not run the constraint pipeline at all, so a `ConnectorBridge` gets no clearance check.
- ✅ **A bridge corridor's middle junction is between its two ends (fixed 2026-09-05; WP-B1's §7.3, plan §8)**: `ConnectComponentsPass._createBridgeCorridor` computed the mid position — the `RandomSource` draw for its perpendicular offset included — and **never assigned it**, so the junction stayed where a fresh `StreetPoint` starts: the **cluster origin**. Nothing failed — the graph was connected and both halves were ordinary `ConnectorBridge` strokes — and the only symptom was two enormous roads through the middle of the city. **The draw was already being consumed**, so the random sequence does not shift and the network's shape and size are untouched; this moves one junction's coordinates and nothing else. **`seed017@2400` moves and nothing else does**, exactly as predicted: `n=785,s=1034,h=27B690F7094A1DE5` → `n=785,s=1034,h=A3ACCA494D9A7A3B`, and it is a **genuine geometry change rather than a re-hash** — measured, **2 canonical stroke lines of 1034 differ**, both the corridor's own halves, the other 1032 byte-identical, `n` and `s` unchanged. The mid moves **1178.3 m**, from `(0.0, 0.0)` to `(-841.9, 822.4)`, and the **318.0 m** gap it bridges goes from **1341.7 m + 1050.3 m** to **165.4 m + 165.3 m**. `street-geometry.json` records no entry for this seed, so no geometry baseline moved, and `StreetCostTests` stayed inside its gate. ⚠️ **"The mid is not at the origin" is the shape of the symptom and NOT the property**, and it is wrong in both directions: a corridor whose ends straddle the origin has its mid there legitimately, and this seed's corridor is a kilometre away from the origin, so an origin check would have passed there for a reason unconnected to the defect. The assertion is that the mid stands **between** its two ends — its projection onto the chord is the chord's own midpoint — **and off** that chord by the offset drawn for it; **both halves are needed**, because the on-origin fixture satisfies the first on its own while the offset is still being thrown away. **Mutations: dropping the assignment fails 3, assigning the plain chord midpoint fails 3, and flipping the SIGN of the offset survives all nine and always will** — which side a corridor bows to is not a property of anything — and is caught only by the recorded fingerprint. Tests: `tests/JoyceCode.Tests/engine/streets/ConnectComponentsCorridorTests.cs` (9), one of which records that **no other pinned seed reaches the corridor branch**.
- ✅ **WP-B2: the candidate queue drains heaviest first, and a junction has a way out (2026-09-05; plan §9)**: D2 removed the arterial stage, the re-seeding walk and the third rule table from Phase B, so **the queue ordering is the entire mechanism** by which a structure will get placed on a heavy corridor before side streets attach to it. `engine.streets.generation.CandidateQueue` replaces `Generator`'s `List<Stroke>`; **`HeavyFirst` false makes `Pop()` `RemoveAt(Count - 1)` on the same list**, not a lookalike, and `HeavyFirst` true scans that list **backwards** for the greatest weight so the most recently pushed still wins a tie — the queue is still a stack *within* one weight, which is what keeps a split's head ahead of its tail with no change at that call site. **`StrokeStore.RemovePoint` exists and the point of it is the OCTREE**: dropping the `_listPoints` entry alone leaves a junction that no stroke touches still answering `FindClosestBelowButNot` and `GetClosestPoint`, so candidates snap onto it and the streets built from them end nowhere — **worse than no removal primitive at all, because it looks correct**. `PolishStreetPoints` has done exactly that since it was written and now goes through the primitive; it gets away with it only because it runs after `Generate()` has returned. `Generate()` is now `_drain()` plus **one** `_connectPass.Run()`. ⚠️ **THE FINDING: no pinned seed reaches the `maxGenerations` budget, so one of the two calls being hoisted was reached by nothing.** `StreetDeterminismTests` has claimed since it was written that `Yelukhdidru@3000` exercises the cut-off; measured, its `_generationCounter` finishes at **1886 against a budget of 9000**, `seed017@2400` at 1034 of 5760 and `seed000@1500` at 365 of 2250 — all eight leave by the queue running dry, and **deleting the budget exit's copy of the connect pass would have passed every gate in this repository**. That is §7's *"a rule can be invisible to unlimited real data"* with an **exit** in place of a ramp. The fixture exists because **the budget and the growable area are independent** (`Size²/1000` versus `SetBounds`): a 200 m cluster growing inside a 2 km square hits its budget of 40 in a few dozen strokes. **B2.2 is asserted on the order candidates actually leave the queue during a real run** (`Generator.OnCandidatePopped`), not on the comparer — a comparer test passes with the queue unwired, which is how `ClearanceConstraint` and `SpanLengthConstraint` kept passing tests for months while sitting outside the pipeline — and it needs three things to have teeth: the same measurement flag off as a control (a lighter candidate is popped ahead of a heavier one on **5–20 %** of pops), a floor on the branch count, and `TheOrderingChangesTheCityThatComesOut`, since an observer could be wired to a queue the accept loop ignores. **NO BASELINE MOVES**: `street-fingerprints.json`, `street-geometry.json` and `street-cost-baseline.json` are byte-identical; `street-fingerprints-gradesep.json` is new and records V2 with the flag on, per seed. **Mutation survivors: none of thirteen** — setting `HeavyFirst` unconditionally fails **44**, putting the connect pass back on the queue-empty exit alone fails **3, all of them new**, and leaving the octree entry in `RemovePoint` fails 3 including an end-to-end control in which a growing street is offered the chance to snap onto a removed junction. Tests: `tests/JoyceCode.Tests/engine/streets/{HeavyFirstOrderingTests,RemovePointTests}.cs`, `StreetDeterminismTests` (55 new; 1275 xUnit against 1220, TALE 200/200). **Deliberately NOT in WP-B2**: any structure placement, `OverpassBuilder` changes, the interior-T-branch rule, and the `ClusterStorage.DbVersion` bump.

- ⚠️ **`joyce.DisableClusterFlattening` DEFAULTS TO TRUE since `1c54a9e4` — the terrain-following city IS the shipped city, and the "flat city stays bit for bit unchanged" invariant no longer describes the default.** Part 4 of the ledger is done. The flat path still exists behind the flag and its tests still pin real properties (mostly *"the height seam is the only thing that decides height"*) — do not delete them — but you are protecting *a* baseline, not *the* baseline. State what each city does under your change.
- ✅ **The citizen keeps to its own pavement (fixed 2026-09-03; ledger item (k); §7p)**: the last plan-level defect the ledger carried as *found and not fixed* — *"roughly half the loop walker's waypoints are outside their own block"*. It is the loop `QuarterLoopRouteGenerator` builds for every citizen `CharacterCreator` makes, i.e. the commonest walker in the game, and it is **identical in the shipped flat city**. ⚠️ **The ledger and §7m had the angle condition BACKWARDS.** The waypoint was `corner + 1.5·n` along the inward normal of the edge **leaving** the corner, with the **arriving** edge never consulted, so it is on the arriving edge's inward side exactly when the two inward normals agree — dot product `−cos θ` — i.e. inside for **θ > 90°**. **The ACUTE corner is the failure.** Measured over the four baselines on the shipped terrain (2918 of 2936 corners; the 18 excluded are §7o's `dist2 > 4000` near-collinear fallback): **0 of 1243 acute corners inside, 1667 of 1675 obtuse ones inside**, and all eight exceptions are corners of **90.000–90.002°** — which is the second thing the symptom could not show: **at exactly 90° the waypoint lands ON the kerb line**, and inside/outside there is a rounding decision, not a fact. ⚠️ **"The rest stand 1.5 m into the carriageway" was also wrong, and the percentage was the wrong statistic**: the **median waypoint was on the kerb line** (+0.00…+0.11 m signed, 14–38 % of them within 5 cm) and the worst excursion was **1.15 m**, not 1.5 — while along the walk rather than at its corners, **10–17 % of positions were outside**. ⚠️ **Reusing §7k's pavement inset ring — the obvious fix and the one proposed — does NOT work, and both reasons were measured.** `SidewalkRing`'s points belong to **edges** and deliberately not to corners (that is its whole design), and a loop turns corners: joining them cuts every corner, and at the **6–16 % that are reflex** the cut leaves the block — outside at 0.0–1.2 % of path positions by up to **11.07 m** taking one point per corner, 0.0–0.3 % by up to **6.20 m** taking both points of each edge, against **0.0 %** for what landed. And **`SegmentNavigator` cannot take two waypoints per edge at all**: it uses `PositionDescription.QuarterDelimIndex` as a *segment* index and then does `Quarter.GetDelims()[QuarterDelimIndex]`, so doubling the waypoints indexes a delimiter list of `n` with a number up to `2n−1`. **What landed is the corner's own MITRE** — `engine.streets.generation.PavementWalk`, the point one offset from **both** edge lines on the inward side of each, reusing `SidewalkRing.MitreOf`/`InwardNormalOf`/`SignedArea2Of`/`ContainsInPlan` (`MitreOf` was hoisted out of `_mitreReach`, which was already computing exactly this vector to place §7k's corner ramps). **§7k rejected the mitre for the pavement SURFACE and that reasoning does not carry over**: a shared mitre vertex is bad in a mesh because the two rim cells want two different heights for it, and a walker is a point with exactly one height. Result: **100 % of waypoints and 100 % of sampled path positions inside their own block on all four cities**, and because both ends of a segment are one offset from the SAME edge line, **the walk between two corners runs exactly parallel to its own kerb** — perpendicular distance ≤ the offset at all 32 098 samples. **The offset is `min(1.5, SidewalkWidth/2)`**: half the pavement so the walker is *on* it, capped at the 1.5 m that shipped, so on the 4 m and 6 m pavements carrying 67 % of the baselines' corners **it is unchanged**. ⚠️ **No baseline city contains a 1 m pavement at all** (0 of 2918 corners — `SidewalkWidth` is 1 m only below downtownness 0.2), so the narrowest pavement the game can build is untestable from real data — §7o's `seed008` lesson — and is covered by fixture. **No fallback for a refused block is needed** (the mitre needs only a corner and its two edges, unlike the inset ring, which is refused on 1/0/3/7 blocks), though the point is still *checked* and a block too narrow to stand off the kerb keeps the kerb line; that branch fires on 0 baseline corners. **THE DEFAULT FLAT CITY MOVES: every citizen's walk**, median 1.13–1.50 m in plan, p95 3.2–3.5, worst 4.11 m, and **exactly 0.000 m at a collinear corner**, where the mitre reduces to the shipped expression — sixth deliberate move, after §7i, §7j, §7l, §7m and §7n. **Nothing moves in height** (asserted as equality: a flat block's edge interpolation is `h + t·0`), and no network fingerprint or `street-geometry.json` baseline moved. ⚠️ **§7m's height table for this walker was CONDITIONAL**: its ±0.23 m covered only the 59–70 % of waypoints that landed on a block floor at all — the rest were over the road. Every waypoint is on the floor now, p05 −0.17…−0.28, p95 +0.12…+0.29; the residual is §7k's corner ramp, and the corner's own junction height was re-measured there and is worse (p05 −0.44…−0.60). **Mutation survivors: two of twenty, both provably EQUIVALENT rather than holes** — `MitreOf`'s two normals swapped (the expression `w(n₀+n₁)/(1+n₀·n₁)` is symmetric, so no output can differ) and its zero-denominator guard removed (the non-finite result is refused by the `IsFinite` check on the next line). One lesson, the old one in a new coat: *"the zero-length-edge guard is gone"* survived its first attempt because the test asserted the walk was *inside* the block, and a guessed direction still lands inside a big convex block — it asserts **equality with the corner** now, since a containment test cannot tell a guess from a refusal. **No source scan was needed anywhere**: both files are in Joyce and are driven directly over whole generated cities. **Two existing gates superseded, not re-baselined, old text recorded in §7p**: `ASegmentStartsBesideItsOwnCorner`'s `< 1.51f` becomes the block's own `SidewalkWidth`, and `AFlatCityLoopIsUnchangedFloatForFloat` becomes `AFlatCityLoopMovesInPlanOnlyAndNotInHeight`. Tests: `tests/JoyceCode.Tests/engine/streets/PavementWalkTests.cs` (15), `builtin/tools/QuarterLoopRouteTests.cs` (25). **Found and NOT fixed**: ⚠️ `SegmentNavigator` indexes a block's delimiters with a segment index, which is already wrong for every route that is not the block loop — `GoToStrategyPart` hands it a `StreetRouteBuilder` route together with the citizen's own pod, so a route longer than its block has corners indexes past the end of `GetDelims()` and a shorter one starts partway along itself; and `PedestrianRoute.SidewalkOffset` is still a constant 1.5 m, so the satnav walker keeps the **width** half of this defect (not the side half, which §7g fixed).
- ✅ **The road mesh represents its own surface (fixed 2026-09-05; ledger item (n); §7s)**: §7r's one *found and NOT fixed*, and the last thing between the satnav guideline and the carriageway it is drawn on. ⚠️ **The framing this started from was wrong in two independent ways, and one COUNT refutes the first.** *"The error is concentrated where a row STRADDLES a kink"*: **0 of 4 608 row spans over five cities contain a section point strictly inside them**, and they cannot — `_generateStreetRun` runs its rows from `damax` to `dbmin`, which is inside **both** sides' climbing windows by construction. So *"a row AT each break"* was not the fix. Nor is it row length on its own: a carriageway is a **ruled** surface between two kerb chords of different slope, i.e. a hyperbolic paraboloid, and a row quad's two triangles depart from it by **`length × |slopeRight − slopeLeft| / 4`** at the midpoint of the diagonal they share — a diagonal ridge across the road, one-signed over the whole quad. Prediction and measurement agree to three decimals at every percentile: **per row quad median 0.039 m, p95 0.25, worst 1.000**. ⚠️ **The worse half is the one nobody had looked at: the END WEDGE**, the single triangle between a junction's *seam* (the line joining its two section points, beyond which there is only the flat cap) and the first full-width row — **median 0.064, p95 0.36, worst 0.898, worse than the rows at every percentile**, and 30 % of a carriageway's triangles. **It needed no geometry at all**: its three corners are fixed by the seams, three corners admit exactly one plane, so the *model* was wrong and `RoadSurface.SurfaceHeightAt` now answers with that plane (and with the junction's height beyond the seam, which the plane itself gives *along* the seam, so they join without a step). **0.00003 m after, at zero vertex and zero index cost.** The rows are bounded instead: **`RoadSurface.MaxSag = 0.02 m`** — one fifth of `RouteRibbon.Lift`, the only derived number in this chain, and below the 16-bit depth quantum at 36 m — with `MaxRowSpan = 4·MaxSag/|slopeR−slopeL|`, **infinite exactly when the two slopes are equal**, which is every stroke of a flat city and every straight stroke anywhere. After: **every row quad ≤ 0.0200 m**. ⚠️ **§7r's own claim that "the residual is the ROAD's tessellation, not the ribbon's" is WRONG**: with the road held to 0.02 m the guideline was still **0.85 m** off it at the worst position and 0.22 m at p99, all of it its own 4 m quads — so `RoadSurface.MaxSpanAcross(across)` generalises the bound (a strip covering fraction `f` of the width carries `f` of the twist) and `RouteRibbon.BreaksAlong` uses it. ⚠️ **And four section points are not enough breaks for a strip**: a junction's seam runs across the road at an angle, so a strip down the middle crosses it strictly between the two section points and **each of its two edges crosses it somewhere else again** — `RoadSurface.BreakpointsBetween` returns **six**, which took the worst case from 0.85 to 0.29 m. **The two tessellations bound themselves independently rather than one deriving the other's row positions** (that is the §7r trap), so the worst case is the sum, `2·MaxSag`. Guideline after, 983 000 positions against the road mesh's own triangles: median **0.0011–0.0070**, p95 **0.018–0.023**, p99 0.028–0.065, worst 0.081–0.386 (plus `seed008`'s 1.25 m filler quad), **more than its own lift below the road at 0.00–0.71 %** against §7r's 3.2–9.5 %. **THE FLAT CITY DOES NOT MOVE AT ALL — every one of `street-geometry.json`'s five recorded cities keeps its vertex count AND its index count exactly**, and no network fingerprint moved; the owner had authorised a geometry-baseline change and the geometry needed none, because a flat city has no slope difference to bound. ⚠️ **Its five HASHES did move, for an unrelated reason mutation testing found: `StreetGeometryFingerprint` hashed the vertex list and merely REPORTED the index count, so swapping two indices of every carriageway row — §7j's culled pavements, in the road's own mesh — passed the entire suite. The indices are hashed now**; old → new are `425494645E2939AC`→`111513A7931F7D25` (seed000@500), `7EBC56BCD8EE7098`→`EAD539C45B34A856` (seed011@500), `D56C2DBE869CC644`→`3186E65937B8BDF3` (Yelukhdidru@800), `14DBCA63B9A9D93E`→`A1E2E34665C544B6` (seed000@1500), `A692D41F9EC531C1`→`1EDB46E33561F333` (seed008@500), with `v=`/`i=` identical on all five — which is itself the evidence that no geometry moved. Every **ramp** is likewise unchanged float for float (`OverpassBuilder` builds them straight, so both sides span the same window). **Cost, terrain city**: road vertices 370/350/972/4 978/26 010 → **638/510/1 776/10 568/50 382** (+46…112 %), worst FRAGMENT 748 → **1 448**; guideline **median 4 quads per lane, max 40** against 3 and 5. Both subdivisions carry an arithmetic cap of 64 and **neither binds** (51 rows, 40 quads observed), asserted strictly below it. **Three traps handled and verified rather than assumed**: the extra rows are *inside* one texture length and keep the same `vStart`, so `uvp.GetUV` tiles the texture exactly as before (the loop used to increment it per row, which was the same thing only while every row was a whole tile — the mutation that increments per sub-row is caught); rows are shared *within* a subdivided span but still duplicated *between* spans, since sharing across spans would change the un-subdivided vertex list; and the last sub-row is `nextD` itself rather than a fraction of the span, so an undivided row is the same float (the mutation is caught by `street-geometry.json`). **Mutation survivors: two of twenty-nine, and the four killed on the SECOND attempt are the finding** — (1) the index-hashing hole above; (2) forcing the wedge planes on where two junction footprints overlap passed everything including `seed008`, since the only seed that reaches that branch is the one whose filler quads the measurement already excludes — §7o's *"data you do not have cannot catch anything"* inverted, killed by a fixture asserting equality with the exact blend; (3) giving BOTH edges of the ribbon the same lateral fraction, i.e. one seam crossing instead of two, passed every distribution in the file because it is 0.1 % of positions — killed at both levels by equality with the two exact crossings, §7p again; (4) the truncated last lane's lost `Surface`, which was not in the first round at all and was found by reading §7r's open items. **The two that survive**: taking the ribbon's two edge points at the lane's start instead of its midpoint (the two kerb chords are parallel to within the 0.1 m junction grid, so the lateral fraction is the same anywhere along the lane; the mutation that collapses both edges onto one point *is* caught), and computing the last sub-row as `currD + (nextD − currD)·1` rather than `nextD` — one unit in the last place, unrecoverable from any measurement of the mesh (the row's distance is read back out of a vertex that was built from it) and below the millimetre `street-geometry.json` rounds to by design; written the exact way regardless, for §7n's reason. Tests: `tests/JoyceCode.Tests/engine/streets/RoadTessellationTests.cs` (26 over five cities incl. `seed008`/500), `RouteRibbonRoadTests` adapted. **Two existing gates superseded, old text recorded in §7s**: `ALaneOverAClimbingRoadIsBrokenWhereTheRoadBreaks`'s *"a stroke has only four section points to break at"*, and `TheCarriagewaySurfaceReproducesEveryVertexOfTheRoadItDescribes`'s p99 — ⚠️ **that percentile is how this nearly passed unnoticed**: cutting the rows finer put more vertices on §7o's skew strokes, whose rows and kerbs are two different lines, and moved p99 past its bound without anything having become less true. It now asserts the maximum on clean strokes (**0.00004 m over 63 000 vertices**) and counts and bounds the skew ones (0/2/0/8/12 strokes of 29/30/74/367/1 875). **Found on the way and FIXED**: ⚠️ **`RoutePlan` truncates the last lane of every route and replaces it with a NEW `NavLane`, copying only `Start`, `Length` and `MaxSpeed`** — so the last lane carried no `Surface` and fell back to the chord, i.e. §7r's defect on the segment nearest the destination, and **§7r's own note said the opposite**: *"the lane it belongs to is the real one, so the ribbon's last quad is right"*. It is not the real one. One line; nothing moves in a flat city. **Found and NOT fixed**: ⚠️ the same replacement still loses `KerbSide` and `AllowedTypes`, and the `KerbSide` half is §7g's defect on that one segment — restoring it moves the satnav walker 1.5 m in the SHIPPED FLAT city, so it wants a round that measures it; ⚠️ the seam's obliquity inside one ribbon quad — worth 0.29–0.39 m at the worst position of the two largest cities and 0.09–0.12 % of positions, and the remedy is known and free (that quad's *other* diagonal IS the seam, so splitting along it is exact) but needs a per-quad diagonal choice `AddQuadCornersUV` does not offer, and reordering corners flips the winding; `seed008`'s overlapping junction footprints, now the worst thing in the measurement by a factor of three; §7o's skew strokes, now counted rather than averaged; and `RoutePlan`'s truncation junction.
- ✅ **The satnav guideline follows the road's own profile (fixed 2026-09-05; ledger item (m); §7r)**: reported from play of the shipped terrain city — *"the navmesh being partially below the street. Seems logical to me if we draw navmesh streetpoint to streetpoint, without considering the flat junctions, whereas the street level has a flat junction between."* **The owner's own diagnosis, and it is RIGHT — the first time in this work stream that a first diagnosis has been.** ⚠️ **There is no navmesh in the shipped game**: `engine.joyce.components.NavMesh` and its emission block, `GenerateClusterNavLanesOperator`, and the `//ng.p(...)` vertex emission inside `_generateStreetRun` are ALL `#if false` or commented out. The one thing in the tree that turns a nav lane into geometry is `engine.quest.ToSomewhere._onJunctions` — the **satnav guideline** §7g brought down off the hover height — confirmed by exhausting the callers of `AddQuadXYUV` and `Mesh.CreateListInstance`, not assumed. A car `NavLane` runs junction centre to junction centre and everything reading it interpolates linearly (including the intermediate junctions `NavJunction.Between` inserts every 50 m); the road is **flat–ramp–flat**, because `_generateJunction` fills each junction with a flat fan at one height and §7o then gave each side of a stroke its own chord between its own two section points. Measured over five cities on the shipped terrain at 984 000 positions against the road mesh's **own triangles**: median **0.076–0.191 m**, p95 0.45–0.93, p99 0.57–1.33, worst **2.45 m**, **below the road at 48–53 %** — and **exactly 0.000 m at every percentile in the flat city**, like §7o and unlike everything before it. ⚠️ **A lift is not a licence to be wrong by less than it**: the median error exceeded the whole 0.1 m `Lift`, whose derivation from the 16-bit depth buffer (§7g) is unchanged and now written where it can be violated. **The fix is the §7l/§7o/§7p pattern**: `NavLane.Surface` carries the `engine.streets.generation.RoadSurface` its stroke's carriageway was **emitted from**, so ribbon and road agree by construction — and the four section points bounding a carriageway, thirty lines of angle-array indexing inside `_generateStreetRun`, are hoisted into `RoadSurface.TryCornersOf` so both read the same ones. After: median **0.002–0.021 m**, p95 0.16–0.25, p99 0.25–0.46. **One quad per lane becomes one per straight piece of the road** — `RouteRibbon.BreaksAlong` subdivides at the four section points clipped to the lane's span, which is exact rather than merely finer; cost **median 3 quads per lane, max 5**, i.e. 12 vertices instead of 4, and a quad stopped being a *parallelogram* (`joyce.mesh.Tools.AddQuadCornersUV`) because the road's cross fall varies along a stroke — measured between the two kerbs at 0.10–0.15 m median and up to **3.59 m**. **The lateral fraction is the ratio of the distances to the two kerb CHORDS, chosen by measurement**: the obvious alternative (offset off the centre line over the half width) is 2.5–3× worse (p95 0.29–0.53 m), because the chords *are* the lines the surface is emitted between. ⚠️ **The residual is the ROAD's tessellation, not the ribbon's** — ⚠️ **CORRECTED 2026-09-05 by §7s: it is BOTH, and the ribbon's own share was the larger of the two once the road was fixed.** The half that was right: `RoadSurface` reproduces all 32 680 emitted carriageway vertices to 0.000000 m median / 8·10⁻⁶ p99, while a carriageway's rows are `StreetWidth()*4` — **up to 88 m** — long and each is two triangles, so the drawn road departed from its own surface by up to **1.00 m mid-row**, invisible at the kerbs (§7o measured 0.003 m there) and invisible in a flat city. The half that was wrong: with the road held to 0.02 m the guideline was still **0.85 m** off it at the worst position of five cities, all of it its own 4 m quads. **The PEDESTRIAN ribbon does NOT share the defect and the two are not symmetric**: a pavement lane's chord IS the block floor's outline, identically — 105 000 samples, **2.3·10⁻⁵ m worst** — so `Surface` is set on car lanes only and `RouteRibbon.SurfaceHeightAt` refuses it for a pedestrian ribbon explicitly; a crossing is level at its junction's walking height, one kerb above the cap, deliberately. **No other lane-Y consumer is affected**, searched: cars hover on a raycast (§7b/§7f), the citizen walker uses `PavementWalk`/`BuildingFooting` (§7p), the satnav walker uses a *pedestrian* lane, `PipeController` is built from the pedestrian network, and lane `Length` feeds a cost. **THE FLAT CITY DOES NOT MOVE** — one quad per lane at the same floats, asserted as `Assert.Equal` over five whole cities (the rail arithmetic is written the old way so it is the same float, not the same place to within a rounding). ⚠️ **One thing in the terrain city does move that is not the ribbon**: `RoadSurface` interpolates through `Single.Lerp` instead of `hA + f·(hB−hA)` so that a corner over a junction cap is that junction's height *as a float* — **7–12 % of emitted road vertices move, by at most 7.63·10⁻⁶ m**, one ulp at 60 m. **No `street-geometry.json` entry and no network fingerprint moved, and no baseline file was rewritten.** **`GenerateNavMapOperator.ContentOf` takes its two stores as arguments now**, which is what makes the nav map drivable at all (its own accessors trigger generation through the `I` container). **Mutation survivors: five of thirty-eight, all killed, four naming something real** — (1) drawing only the FIRST quad of each lane passed everything, since the loop was at the scan-only call site and **a scan sees the name of a call, not how many of its results are used**; the loop moved into `RouteRibbon.MeshFor` and the gate compares mesh vertices to quads **in order**, which also kills a corner swap that keeps the count and faces the ribbon away from the camera (§7j); (2) dropping the cluster offset from `RoadSurface.OfStroke` passed everything because **every generated city in every test sits at `Pos = Vector3.Zero`** while the shipped world puts 70 cities at a median 36 km out — §7n's own survivor again; (3) `MeshFor` ignoring the transport type; (4) the `0/0` guard on the lateral fraction, reachable only where the two kerb lines CROSS (a zero-width road does not reach it — the equal-heights shortcut answers first) and asserted there as equality with the exact blended height, §7p's *"a containment test cannot tell a guess from a refusal"*; (5) deleting `_generateStreetRun`'s `ErrorThrow` for a malformed junction, unreachable from any generated city and pre-existing, now covered by a fixture on `TryCornersOf` plus a scan. Tests: `tests/JoyceCode.Tests/builtin/modules/satnav/RouteRibbonRoadTests.cs` (60 over five cities incl. `seed008`/500), `RouteRibbonTests` adapted, `KerbSeamTests.OnlyOneExpressionSaysWhereACarriagewayBeginsAndEnds`. **Found and NOT fixed**: ⚠️ the **88 m rows** above — the road mesh does not represent its own surface — ✅ **fixed 2026-09-05, see the §7s entry above; and "shortening them moves `street-geometry.json` for every recorded city" was wrong too — nothing in it moved**, because a flat city has no slope difference to bound; `seed008`'s overlapping junction footprints, where two caps give the road two heights 1.25 m apart at one place; and `RoutePlan`'s truncation junction, whose synthetic `GroundHeight` is the chord's rather than the road's (nothing reads it).
- ✅ **A junction corner is on both of the streets that meet there (fixed 2026-09-03; ledger item (l); §7q)**: §7o's last *found and not fixed*, and **the diagnosis it carried was wrong — as was the one this round started from.** A junction's section array holds one point per adjacent pair of arms, the **mitre** of their two carriageway edges, and everything hangs off it: the junction cap's corner, the block's corner, the end of the carriageway, and through the block outline the estate, the `ClipperOffset` footprint, the building and its shops. ⚠️ **`_computeSectionArrayNoLock`'s `dist2 > 4000` fallback is INNOCENT and its code comment was right.** Measured over five generated cities and 4552 section points, **all 46 corners that take it and all 427 that take the parallel branch are at 179.9999–180.2983°** — the *straight-through* case it calls *"pretty in-line streets"*, not a hairpin — and the averaged offset it substitutes lands **on** both edge lines there, worst residual 1.13 m. The proposed "it normalises a near-zero vector" is refuted by one number: `|nc − np|` is **2.0000, its maximum**, in every one of the 473 cases. **The defect is `geom.Line.IntersectInfinite`**, which solves by Cramer on homogeneous line coordinates whose constant term is `A.Y*B.X − A.X*B.Y`: 3 km from the origin that is 2·10⁶ and the solve forms a difference of two products of 2·10⁸, so two nearly parallel lines cancel away every significant digit. The answer comes back **6.3–56.3 m from the junction — inside the 63.2 m guard, so accepted — and up to 27.3 m off both of the lines it is the crossing of**; a guard that tests only *how far away* an answer is cannot see a cancelled one that lands nearby. `engine.streets.generation.SectionMitre` answers the corner from **two unit normals and two half widths and never forms an absolute coordinate**: `S/sin(θ/2)` along the bisector plus `D/cos(θ/2)` across it, whose two terms blow up in two different places (θ→0, a real hairpin; θ→π with unequal widths, where two parallel offset lines a width apart have no crossing) and neither of which is numerical. The symmetric term **is** `SidewalkRing.MitreOf`, hoisted by §7p and reused; its denominator `1 + n₀·n₁` is `2sin²(θ/2)`, well conditioned exactly where the intersection is not. Off-line residual **0.00001–0.00006 m at every percentile of every city**, against a worst of 27.3 m. **The bound is now relative to the width** — `dist2 > 4000` is 63.24 m, a mitre limit of 6.7 on the narrowest street and 13 on the widest — **and it is 3, NOT Clipper's default of 2**, measured: 2 cuts back 888 of 7544 corners and leaves **1018 block edges over 0.25 m off their own carriageway**, worse than the defect; 3 cuts back 42, of which 32 are degenerate at any limit. **§7o's three counts: off-line block edges 13→5 over ten cities (11→5 on `Yelukhdidru`/3000, its collinearity improving at every percentile — median 0.00015→0.00002, p99 0.02046→0.00007), footprint reach 58.1→27.5 m worst, uncovered kerb 237→185.** **BOTH CITIES MOVE, seventh deliberate move of the default flat city**: section points median 0.00012 m, p95 0.0095, worst 56.07, 40 of 6084 over a metre; **27 blocks of 445 on `Yelukhdidru`/3000 have a corner moving over 5 cm and six over a metre**, taking their estate, building, shops, TALE locations and nav crossings with them. ⚠️ **`street-geometry.json` MOVED for all five recorded cities** — four by sub-millimetre rounding with every vertex and index unchanged, `seed000@1500` by 12 vertices and 18 indices; old and new hashes are in §7q. **No network fingerprint moved** (the section array never feeds back into the stroke graph). **The BEVEL was costed and not built**: two corners at an over-long junction is the only construction that puts every corner on both lines exactly, but the array's length is contract — `QuarterGenerator` and `GenerateNavMapOperator` index arms and sections together — and it would move every block cornering on such a junction, for ten corners in eight cities. **Mutation survivors: three of seventeen.** (1) the hairpin branch below 1.3° was reached by nothing, so substituting the current arm's own normal — *perpendicular* to the right answer — passed everything; killed by asserting **equality with the bisector**, §7p's *"a containment test cannot tell a guess from a refusal"* again. (2) the final `Single.IsFinite` fallback was unreachable AND ineffective (for a NaN direction its own fallback is built from the same NaN) and was **deleted** rather than tested. (3) swapping `MitreOf`'s two normals survives and always will — it is symmetric. **Two existing gates superseded, old text recorded in §7q**: `KerbSeamTests`' `nOffLine <= 12` is now `<= 5`, and `PlanTolerance`'s comment blaming the fallback is corrected; separately `QuarterFloorTests.ThePadStillAgreesWithTheFloorInTheMiddleOfTheBlock` used `Assert.Equal(…, 2)`, which ROUNDS both sides, so a 2·10⁻⁶ m move took 18.3149986 and 18.3150005 to opposite sides of 18.315 — it asserts `|Δ| < 0.005` now. Tests: `tests/JoyceCode.Tests/engine/streets/SectionMitreTests.cs` (32). **Found and NOT fixed**: ⚠️ the **five** remaining off-line block edges are a *different* defect — the block ring **skips a junction**, so `delims[i].Stroke` is not the street that edge runs along (stroke 751 from #586 to #656 followed by a corner on **#655**, and two more like it); kerb coverage went slightly UP on two of ten cities, identically at limits 3/4/6, so it is the corrected corners rather than the bound; and `geom.Line.IntersectInfinite` is unchanged, with one caller left in the tree (`IntercityTrackElevationOperator`, a boolean rectangle test).
- ✅ **The kerb rests on the carriageway (fixed 2026-09-02; ledger item (j); §7o)**: reported from play of the now-default terrain city — *"I still can observe a small gap between the bevel of the sidewalk and the street."* There is no sidewalk object: the block outline is extruded up by `QuarterSidewalkOffset`, so the top face is the pavement, the sides are the "bevel", and **the outline is the line where two independently generated meshes are supposed to meet**. **This is the first item in the ledger that is EXACTLY ZERO in the flat city** — 0.000 m at every percentile of all four baselines — so it could not have been seen before the flip, and establishing that was the single most useful measurement. **Three of the four candidates are not it.** Not a plan gap: a block corner is a section point of its junction and the two section points bounding one stroke lie on the same offset of its centre line, so kerb line and carriageway edge are collinear to **0.0002 m at the median, 0.02 m at p99** over 2936 edges. Not a missing face; not z-fighting (half a metre is 13× the 16-bit depth quantum at 50 m). **It is the junction footprint**: `_shearOntoSlope` lifted the road by ONE window along the stroke's **centre line** — flat over each junction footprint, climbing between — while the kerb is a straight chord between two **section points**. The two agree at both ends, which is exactly why §7c could fix the corners and leave this, and disagree in between: p05/p95 **−0.57 / +0.55 m**, worst **6.49 m**, exceeding the whole 0.15 m kerb at **27–31 %** of positions and half a metre at **11 %**, sign symmetric. **`engine.streets.generation.RoadSurface` gives each SIDE its own interpolation between its own two corners** — exact, not close: at either end the chord parameter is 0 or 1 (so a corner still carries its junction's own height, the property the window existed to protect), and in between the axial coordinate is an **affine** function of position along the chord. Residual **0.000–0.003 m** worst over five cities × four grounds. `RoadSurface.HeightAtJunction` is also now the ONE expression for a junction's road height, replacing **five** copies — two written with `MetaGen.ClusterStreetHeight` and three with `CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE`, **two different constants that are both 2.0**, and two of the five dropped the deck term. **THE FLAT CITY DOES NOT MOVE and neither does any ramp**: the shear returns before touching a vertex when the two junction heights are equal, and a straight junction puts both section points at the same axial distance so both sides share one window and the rule reduces to the old expression; `street-geometry.json` and the network fingerprints are unmoved. **Mutation survivors: two of fifteen, both then killed.** (1) deleting the shear in the *overlapping-footprint* branch — which never sheared at all — survived because **no baseline city contains such a stroke**; killed by adding `seed008`/500, the seed `StreetGeometryTests` found by instrumenting that branch. §7j's `PartitionContains` lesson again: data you do not have cannot catch anything. (2) removing the clamp on the chord fraction survived and provably always will from the operator's side (every emitted vertex lies inside its own side's span), so it is a bound on what a *caller* may ask; killed by `RoadSurfaceTests`. **Two existing gates were superseded, not re-baselined, and their old text is recorded in §7o**: `HeightRisesMonotonicallyAlongABentStroke` sorted *every* vertex of a bent stroke into one monotone sequence — the two sides no longer span the same window, so it is now per side; and `TheSlopeNormalMatchesTheGradientOfTheSurface` measured one gradient across both sides (8.9 % against the 13.1 % each side carries) and is now per side too. Tests: `tests/JoyceCode.Tests/engine/streets/KerbSeamTests.cs` (27). **Found and NOT fixed, all plan-level, all one root cause**: `StreetPoint._computeSectionArrayNoLock`'s `dist2 > 4000` fallback for near-collinear arms leaves **11 of 2477 block edges of `Yelukhdidru`/3000 not on their own stroke's edge at all** (up to 62 m off), lets two strokes' carriageways **overlap by 45 m** so "how high is the road here" has two answers 1.6 m apart, and leaves 0.2 % of kerb positions uncovered by their own carriageway (identical in the flat city). Moving a section point moves the block outline, the estate, the building and its shops.
- ✅ **The quest marker rests on the road, and the citizen on the pavement (fixed 2026-08-31; ledger items (b) and the rest of (d); §7m)**: two things that STAND on the city, both asking the TERRAIN how high the city is.
  **(b) The marker had two causes and neither is sufficient alone.** `ToSomewhere._createTargetInstance` drew the goal cube scaled to `(SensitiveRadius, 3, SensitiveRadius)` **centred on** `RelativePosition`, so its visible bottom was always 1.5 m below the chosen height; and the three quest strategies chose `Loader.GetHeightAt + ClusterNavigationHeight` — the **terrain** plus the **vehicle hover** clearance, neither of which is a surface. The flat city hid it by coincidence: `ClusterBaseElevationOperator` writes `aver + 1.5f`, a constant unrelated to `CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE = 2.0`, so the bottom landed exactly 1.0 m over the road. Measured over every junction of the four baselines on the shipped terrain (conform pass reproduced on its own 20 m grid), **the bottom was below the pavement at 88–93 % of junctions, median −0.65 m, worst −9.77 m** — and, in the other direction, **+10.6 m at the worst junction**, where the road is in a cutting the grid cannot cut. ⚠️ **The ledger's "cheapest and honest" option (C) — just rest the cube on its existing anchor — does NOT meet the requirement, and that was measured before it was discarded**: it still leaves the bottom 2.67 m under the pavement at the worst junction of two baselines and **8.27 m** at the worst of `Yelukhdidru`/3000. Option (A), `GetNavigationHeightAt`, is the same terrain quantity again and changes nothing on a slope. So **(B), with (C) as its mechanism**: `engine.quest.QuestMarker` owns the cube's height and the offset that rests it on its anchor (one copy of the 3 m, since the offset is half of it), and `engine.streets.generation.CitySurface` answers the built surface at a plan position **from the junction nearest it** — `JunctionCollider.SurfaceHeightOf` plus `QuarterSidewalkOffset`, because the pavements cornering on a junction are one kerb above its carriageway. A quest destination IS at a junction (`Placer` with `Reference.StreetPoint` adds `sp.Pos3` and nothing else), so the answer is exact and is asserted by **identity** (`Assert.Same`), never by distance. **Deliberately NOT `max(surface, terrain)`** — the §7f hover-probe shape would float the marker 9 m over the road inside a cutting. **THE DEFAULT FLAT CITY MOVES: every quest marker drops by 0.85 m**, from hovering a metre over the road to resting on the pavement (fourth deliberate move, after §7i, §7j, §7l); the goal's 1000 m collision cylinder drops 2.35 m with it and still spans everything; and `TrailVehicle`'s car-parented marker now stands ON the car instead of around it.
  **(d1) Naming a driver is not having one.** All six `EntityCreator` sites name a factory and one still had **no animation at all**: niceday NPCs start in `RestStrategy` → `NearbyBehavior`, which drives the "E to Talk" prompt and never called `SetAnimation`, so their whole animation was the unretried `InitialAnimName` one-shot. **The criterion CLAUDE.md credited the missing drift test with would have passed that site.** `nogame.characters.citizen.AnimationDriver` is the retry, extracted out of `IdleBehavior` and now used by it, by niceday's `NearbyBehavior` and by a new `AnimationOnlyBehavior` for the **taxi passenger** (which has no `Body`, so `IdleBehavior` is unusable there). `EntityCreator`'s catch now **dooms** the half-built character as well as hiding it — the stated reason not to ("risks a double dispose") expired on 2026-08-29 when `DoomedEntitySet` made dooming idempotent, and hiding alone leaves a visible T-pose forever if `SetVisible` itself throws.
  **(d2) One of the three causes was already exactly zero.** Re-measured at the point a walker stands: the **satnav walker is 0.00 m off the block floor at every percentile, 0.0 % below**, on all four cities — §7k fixed it outright, the only residual being the 1/0/3/7 blocks that get no pavement inset. The **loop walker** (the ordinary city citizen) took the block's **pad** and is worse than the ledger said, because the ledger sampled edge midpoints and the walker stands at corners: p05 **−6.55 m**, worst **−17.78 m**, below the floor at 45–60 % of corners. It takes `BuildingFooting.PavementHeightAt` now — p05 −0.23, median 0.00, p95 +0.23. ⚠️ **The obvious alternative was measured and is worse**: the corner's own junction height (literally the satnav walker's number at the same corner) leaves it below the floor at 33–55 % of corners, because the waypoint is 1.5 m in, inside §7k's corner ramp. The **terrain walker** carried the comment *"the terrain has to answer here, since there is no road node to ask"* — **false**: `TryCreateCursor` has already snapped both route ends to their nearest lane, so `PedestrianRoute.EndWaypointFor` takes the height off that lane at the position's own projection and **nothing on a walker's route is a terrain sample any more**; `GoToStrategyPart`'s straight-line fallback asks the pod's own block through `BuildingFooting.TryPavementHeightAt`. **The flat city is otherwise unchanged float for float** (the loop route's positions and heights are asserted as exact equality; a flat block's edge interpolation is `h + t·0`). **Mutation survivors: three of sixteen, all fixed, and each named something real** — (1) `if (false)` round `GoToStrategyPart`'s pavement branch passed everything, since a scan sees the NAME and not whether the branch is taken, so the decision moved into `BuildingFooting.TryPavementHeightAt` where a test can drive it; (2) gutting `IdleBehavior.Behave` survived the reachability guard (`WalkBehavior` is in the same closure) and then survived a second time because the scan tested for the string `AnimationDriver`, which the field declaration still contained — **an animation driver is a CALL, not a mention**; (3) `Loader.GetCitySurfaceHeightAt` reverting to the terrain passed everything, since `Loader` needs the container and the elevation cache — brace-scanned, the same shape as §7b's `JunctionCollider.SurfaceHeightOf`. A fourth lesson, found while writing the drift test: **identifiers in COMMENTS leak a source-scan closure** — a stale class comment in niceday's `EntityStrategy` naming `WalkStrategy` walked the closure into the citizen strategy tree and let the broken site pass. **An existing gate was superseded, not re-baselined**: `NavJunctionHeightTests.TheRouteBuilderTakesEveryWaypointFromItsOwnLane` asserted the two terrain samples under the claim they "are the only two that do" and have to; it now asserts the same property on the stronger expression and its comment records what it used to say. No network fingerprint or `street-geometry.json` baseline moved. **Found and NOT fixed**: ⚠️ **roughly half the loop walker's waypoints are OUTSIDE their own block** (50–58 % inside) — ✅ **fixed 2026-09-03, see the §7p entry above, where BOTH halves of this sentence turned out to be wrong**: it is the ACUTE corner that lands past the arriving edge, not the obtuse one, and the median waypoint was ON the kerb line rather than 1.5 m into the carriageway. It did move every citizen's walk in the shipped flat city, as predicted. Also: `GoToStrategyPart` can only ask the pod's OWN block; the marker's guarantee is at its own position, not over its 10 m footprint; and `CitySurface` degrades away from a junction, by design and by name. Tests: `tests/JoyceCode.Tests/engine/quest/QuestMarkerTests.cs`, `builtin/modules/satnav/PedestrianRouteEndTests.cs`, `builtin/tools/QuarterLoopRouteTests.cs`, `engine/joyce/CharacterAnimationDriverTests.cs` (which **now exists** — see below).
- ✅ **The intercity tram rides its own track; a new game starts where its coins are (fixed 2026-08-31; ledger items (e) and (f))**: two reports — *"Are they flying like 20-30m above ground?"* and *"the initial sequence of coins not placed next to the initial (newgame) player position."* **Neither is a terrain defect: both are identical in the shipped FLAT game**, and both moved it. ⚠️ **The city tram is NOT the one, and its one known tail is now gone as well**: `characters/Tram/Behavior.cs` samples conformed terrain per frame and, re-measured at nine points along every stroke of the four baselines (21105 samples), runs median **11.01 m** above the road with minimum **+1.88 m** and **0.0 % of samples below it** — the ledger's recorded "min −1.5 m" was removed by §7a and §7d, not by this work. **The intercity tram was the defect.** `characters/intercity/GenerateCharacterOperator` built two `SegmentEnd`s at `ClusterA.AverageHeight + 20` and `ClusterB.AverageHeight + 20` and flew the straight chord between them, while its track — `IntercityTrackElevationOperator`, a flat ribbon at `Line.Height = min(AverageHeight(A), AverageHeight(B))` — is written in a different file from a different expression. Measured over the world the game actually builds (the **real** `GenerateClustersOperator._generateClusterList` seeded `"mydear"`, **70 cities, 114 lines**): `|dAverage|` median **23.53 m**, p95 66.8, max **89.34**, so the vehicle ran median **43.5 m** over its own track at the higher end, max 109.3. `engine.world.IntercityLine` owns both heights now and the vehicle's is derived from the track's. **The 20 m clearance is DERIVED, not chosen** — for two cities of equal average the shipped expression put the vehicle exactly 20 m over its track, so a matched pair does not move by a float; the world contains no matched pair, which is the whole cost. **No per-frame sampling and no navigator change were needed**: a track that is one height everywhere has nothing to sample, so `RouteBetween` levels both ends and the property holds along the entire chord — driven for 600 s through the **real `SegmentNavigator`**, not asserted about two endpoints. **FLAT-GAME DELTA: 114 of the 228 route ends come down** (median 23.53 m, max 89.34 m); the other 114 do not move; **none rises**. ⚠️ **Honest trade: the fix lowers the tram INTO the landscape** — against the terrain the intercity operator does not flatten it was above ground 71.7 % of the time and now is 57.3 %, because the track is median **−13.10 m** against untouched terrain (range −149.3…+134.2 m, 63.8 % cutting) **with no track geometry drawn at all**. **DELIBERATELY NOT BUILT: what an intercity line IS** — embankment / viaduct / elevated line, each costed and measured in §7n, left to the owner. Layer ordering is unaffected (`/000200/intercityTrails` stays above the conform pass; the fix reads what that operator writes). **(f) is three defects, only the first reported.** (1) `DropCoinModule` dropped 19 coins in a column at hard-coded **(164, 45…99, 137)** — **102.05 m** in plan from where the player appears, not "hundreds"; and the column's vertical shape was nearly right (57 m of coins over a spawn that falls 100 m is a thing to fall THROUGH — it was hanging beside the fall), so it is kept, 19 coins 3 m apart, hanging under the start with its top 3 m below it. (2) **The operator could not have asked**: `Saver.CallOnCreateNewGame(object gs)` **never passes `gs` to its operators at all** and `AutoSave.GameState` is null then. `DropCoinModule` is the **only** consumer of `OnCreateNewGame` in the tree, so moving when it runs was genuinely available and was not taken; `engine.world.PlayerStart.Find()` resolves the start **once** and hands the same `StartPose` to the coins and to the player — necessary, because which estate is free depends on which fragments have generated by the time it is asked. (3) **The double add**: `FindStartPosition` answered cluster-relative on the estate branch and absolute on the fallback while both call sites added `cluster.Pos`, so the fallback spawned at `2 × cluster.Pos` — **outside its own city for 69 of the shipped world's 70**, median 36.6 km; the exception is the start cluster, 11.5 m from the origin, so a fixture near zero shows nothing. **The branch is reachable**: `QuarterGenerator` builds on an estate as it traces it and on **seed000/500 it succeeds on all three**, so the smallest baseline city takes the fallback on a freshly generated world. `FindStartPosition(out,out)` is **gone**, replaced by `FindStartPose()` returning a `StartPose` whose one field is `V3World`, so a caller that still adds `Pos` does not compile. **FLAT-GAME DELTA: all 19 coins move 102.05 m in plan and +35.76 m in Y; the PLAYER does not move at all** (`<92.065094, 137.76129, 209.37798>` bit for bit); the **debug cluster beam** drops by each city's own `Pos.Y` (median 22.94 m, max 38.69, exactly 0 for the start cluster — the random nominal elevation `ClusterBaseElevationOperator` later replaces). **One ordering consequence, stated not discovered**: the coin operator now triggers the start cluster's streets, so its `ClusterCompletedEvent` fires during create-new-game rather than at preload — `TaxiNpcSpawnerModule` is subscribed by then, `TaleModule`/`Narration` are not; `TaleSpawnOperator` populates on demand for exactly this reason and `quest.autoTrigger` is a test-only setting. **Mutation survivors: three of twenty-four, all then killed, plus one that survives on purpose.** (i) `Height = 0f * IntercityLine.TrackHeightOf(...)` passed a scan looking for the call — §7m's *"a driver is a call, not a mention"* one turn on: **a call is not an assignment**; both nogameCode call sites are matched as whole expressions now. (ii) adding `clusterDesc.Pos.Y` to the estate branch survived because every fixture used the start cluster, whose `Pos.Y` is 0. (iii) `if (false)` round `PoseIn`'s `IsInvalid` skip survived because all 3/10/82/445 blocks of all four baselines are valid — §7j's `PartitionContains` shape again. (iv) taking the route direction from the raw stations survives and always will: `SegmentEnd.Right` feeds only an orientation the first `NavigatorBehave` overwrites. **An existing gate caught this work and was updated, not relaxed**: `ClusterGroundHeightTests` failed on both new files, which is what it is for; its *stale* half then forced removing two entries that no longer read `AverageHeight`. Tests: `tests/JoyceCode.Tests/engine/world/{IntercityLineTests,PlayerStartTests,IntercityWorldHarness}.cs`. **Found and NOT fixed**: `CallOnCreateNewGame` ignoring its argument; `AutoSave.GameState` being null there; `Line.Width = 5f` not being the width of anything (the written band is `2·stepX` = 40 m either side, so the fine intersection test can reject a fragment the writing pass would touch); and the start estate being a race rather than a place across process boundaries.
- ✅ **A building stands on its block, and its shops stay reachable (fixed 2026-08-31; ledger item (a), RANK 1)**: reported from play of a terrain-following city — *"a building hovering in the air, with grey/white noise on its exposed underside"*, plus the constraint *"shops shall be placed only in a reachable way, so that they are at the same level or above the sidewalk."* **The base is ONE scalar**: `GenerateHousesOperator` handed the footprint to the L-system with `Y` forced to 0 and extruded straight up, so `2.5f + quarter.GroundHeightAt(centroid)` IS the entire floor (the code comment claiming the house "tilts with" the block was false and is gone), and `GroundHeightAt` is the **pad** — a least-squares plane, not the surface. An estate IS the block outline inset by `Quarter.SidewalkWidth` (1–6 m), so the footprint spans the block: measured diagonal median **89.3 m**, max 359 m, with the block floor rising **13.3 m at the median under one footprint** and 52 m at the worst. **Every building in every baseline city had both a corner in the air and a corner in the ground.** ⚠️ **The owner chose PLANAR floors, explicitly** — *"real live buildings usually have planar floors … shopfront entries would be usually aligned per story and not gradually … let's for a moment ditch the stairs and align to stories"* — so a footprint-following base was offered and **rejected**. `engine.streets.generation.BuildingFooting.BaseHeightOf` answers the block's **lowest corner** + `ClusterStreetHeight` + `QuarterSidewalkOffset`, which is a **bound** on the floor rather than a sample of it: an outer cap vertex is `CornerGroundHeightAt` exactly and each §7k rim inset point is a convex combination of its own edge's two corner heights, so a piecewise-linear surface over them cannot go below the lowest corner. **The premise was checked, not assumed** — projection overrun 0.0000 and zero inset heights outside their block's corner range, over four cities × three grounds. **Over the whole block rather than the footprint because a block carries exactly ONE estate and at most ONE building** (1 estate on each of 3/10/82/445 blocks; 3/3/81/149 buildings, never two), which makes the block-wide minimum only **0.19–0.61 m below the exact footprint minimum at the median** (worst 3.74 m). No margin is subtracted: the house's own floor cap is emitted clockwise, faces down and is back-face culled from above, so coplanar costs nothing. **Burial is the accepted price**: `localFloor − base` at a footprint vertex is median **4.9–9.4 m**, p90 8.6–23.3 m, worst 53.9 m. ⚠️ **The half the ledger did not name: sinking EATS the building.** With the design height unchanged the roof of **64 of 149** buildings in `Yelukhdidru`/3000 falls below the block floor somewhere over its own footprint and the median 24 m building shows 4.5 m at its highest corner (none disappears entirely, 0 of 149) — so `BuildingFooting.HeightOf` adds the block's corner **spread**, putting the roof its design height above the HIGHEST corner: median +8.0…14.9 m, max +55.7 m, **exactly zero on a flat block**. **Shops snap to a storey**: `StoreyGroundAt` = lowest corner + whole `MetaGen.StoryHeight` steps until it clears the pavement **in front of that shopfront** (not its building, which spans a block) — storey index median 2–4, max 19, `sill − localPavement` median 1.2–1.9 m and below one storey always. The **shop window**, the **shop POI entity** (which was the only thing on a block asking the **terrain**, `ClusterDesc.GroundHeightAt + 3.5`) and the **TALE shop door** all ask that one function and each still adds its own constant; TALE home doors and building `Position`s now take `PavementHeightAt` at their own position instead of the pad at the **block centre**. **`MetaGen.StoryHeight` is now the one copy** of 3 m (was three literals). **THE DEFAULT FLAT CITY MOVES: every house drops by exactly 0.35 m** (pad + 2.5 → pavement `AverageHeight + 2.15`) — it has floated by that much since the L-system houses were written, hidden wherever a shopfront quad skirted the gap 0.10 m *below* the pavement. Everything else is asserted as **equality**: the storey index is a difference of two GROUND heights so the two constants cancel and it is exactly 0, and the shopfront quad, POI and door land on the float they land on today. ⚠️ **The grey/white underside is NOT an atlas gutter**: the constant `Vector2.One/64f` cap UV lands on a texel whose alpha ≤ 0.8, so `LIghtingFS.frag`'s `renderInterior` runs the **full interior-room raymarch** over a horizontal polygon — and it is **not specific to the underside**, since `ExtrudePoly` gives the ceiling cap the identical UV, plane and material, so every building ROOF in the shipped flat city is the same construction. Deliberately left; the sighting is removed because the base is now under the block floor over the whole footprint. Tests: `tests/JoyceCode.Tests/engine/streets/BuildingFootingTests.cs` (42) and `ShippedTerrain.cs`, which reproduces `GroundOperator`'s diamond-square, `ElevationBaseFactory`'s per-fragment refinement and `CacheEntry`'s own sampling rule inside the test assembly. **Mutation survivors: none of eleven, but two are caught only by a source scan** (the house base and the shop POI live in `nogameCode`, which the test assembly does not reference — the same limit §7j hit); both scans assert the ABSENCE of the old expression as well as the presence of the new one. **Found and NOT fixed**: `GenerateHousesOperator._createLargeAdvertsSubGeo` is complete and **never called from anywhere**; a one-storey building on a slope can carry a shop window taller than its own visible height (rare, and not new). **Fixed on the way**: the five `catch` blocks in `GenerateHousesOperator` were `Trace`, i.e. silent — now `Error(_dc, …)` with a distinct message each.
- ✅ **A block's pavement is level across its width (fixed 2026-08-31; ledger item (c), RANK 1)**: reported from play — *"sidewalks shall be up/downwards only in the direction of walking, not in the direction to the street."* A block floor is a single triangle fan over the boundary ring with **no interior vertices at all** — 3 to 16 vertices carrying a block up to 150 m across — so since the kerb fix, where every corner sits at its own junction's height, that fan is a warped quad and which way each triangle tilts is decided by LibTess's sweep. Measured **within a pavement's own width** on rolling ground it falls **7.5 % ACROSS at the median against 7.0 % along** (p95 16 %, worst edge 63 %) — the surface tilted diagonally at ~45° to the street. ⚠️ **The ledger's recommended repair (Option 2 — one inset vertex per corner, at the mitre, taking that corner's height) does not work, and it was built and measured before being discarded**: a mitre sits one width from BOTH edge lines, so it is `w·cot(θ/2)` along one edge from the corner and the same back along the other, and the two rim cells it serves want two different heights for it. Given the corner's height each cell keeps a cross-fall of `s·cot(θ/2)` — **at the median 90° corner that is `s` itself, i.e. no improvement at all**; over the real cities it moved the median from 7.2 % to 6.7 %. Given either cell's height the surface cracks open by 0.4 m on a 2 m pavement. **What actually decides it**: a rim quad has no cross-gradient exactly when every vertex carries the height the outer edge has *at that vertex's own projection*, so the four heights lie on `h = h₀ + s·x`; the only thing forcing a compromise is one vertex serving two edges, **so the edges do not share one** — each edge owns its pair (`builtin.tools.CapInsetEdge`) and neighbours meet only at the outer corner, where both name the corner's height trivially. Measured: **cross-fall 0.0 % at every percentile on all 2823 edges**, 438/445 and 79/82 blocks inset. The corner ramp must **clear the corner's mitre and then add a width** — at exactly one width the two edges' insets land on top of each other at a 90° corner, which rejected 435 of 445 blocks before it was measured. `Quarter.SidewalkWidth` is promoted so the floor and `_createBuildings` inset by one number. **Flat city unchanged vertex for vertex and index for index** (refused on `IsFlat`). Tests: `tests/JoyceCode.Tests/engine/streets/PavementCrossFallTests.cs`, which measures the cap's OWN triangles barycentrically and carries the plain fan as a baseline so "level" cannot be satisfied by a collapsed surface. `QuarterFloorFacingTests` still tests the plain fan — still what a flat city and a refusing block get — and its scope claim is corrected.
- ✅ **A pedestrian keeps to the pavement in both directions (fixed 2026-08-31; ledger item (g))**: `PedestrianRoute.WaypointFor` offset 1.5 m to the **right of travel** unconditionally, and `GenerateNavMapOperator` creates sidewalk lanes in **both** directions over the same ground — so exactly one lane of every pair put the walker in the carriageway at pavement height, in the shipped flat city too. **A sign flip is not the fix**: both directions share the ground, so the side has to belong to the LANE. `NavLane.KerbSide` is a unit vector in plan toward the block, set on both directions at creation, and **zero for car lanes and for every pedestrian CROSSING** — a crossing is in the roadway by definition and belongs on its centre line. Which side comes from the block's own signed area, not a constant: all 659 baseline blocks are traced clockwise today, so a constant would be right and would silently put every route in the road the day the tracing order changed. `QuarterLoopRouteGenerator` uses `-1.5f * vu3Right` and has always been right; the two are now compared against each other, since that disagreement is the shape of the defect. ⚠️ `NavJunctionHeightTests.TheWaypointStaysOnTheRightHandSidewalk` had been **asserting the defect**. Tests: `tests/JoyceCode.Tests/builtin/modules/satnav/PedestrianKerbSideTests.cs`.
- ✅ **The last row of every fragment's elevation grid is written (fixed 2026-08-31; ledger item (h) — VERIFIED and REFUTED)**: `ElevationBaseFactory` copied its grid with one inclusive and one exclusive loop bound, so `Elevations[20, *]` stayed at a default `ElevationPixel`, height exactly 0. The ledger called this a possible **400 m-pitch cliff grid over the whole world** and asked for verification first. **Both halves of that are wrong.** The drawn terrain never read the row: `CreateTerrainOperator` takes its grid from `Cache._elevationCacheGetRectAt`, which copies local indices `0…gr−1` out of each fragment and takes the shared boundary sample from the **next fragment's index 0**. Nor did any city: every operator above the base refills its whole target from `GetElevationSegmentBelow`, the same stitcher, so the row was written from the neighbour — the hole survived **only outside every cluster**. What it did reach is `CacheEntry.GetElevationPixelAt`, which indexes `elevations[ey+1, ex]` directly, so a point query in the last 20 m strip interpolated between a real height and zero (measured gap > 100 m) — i.e. `Loader.GetHeightAt`, `ClusterDesc.GroundHeightAt`, `GetWalkingHeightAt`, the hover probe's terrain fallback and debris placement all disagreed with the ground that is drawn. Note the missing dimension was the last **Z row**, not a column: the write indexed `[x, y]` while every reader indexes `[ez, ex]`. Tests: `tests/JoyceCode.Tests/engine/elevation/ElevationGridCoverageTests.cs` drives the real `Cache`, stitcher and `CacheEntry` (its operators are confined to an AABB 700 fragments out and queried by explicit layer, never `TOP_LAYER`, so they cannot be selected by anything else in the assembly).
- ✅ **The pavement faces upwards; "very few sidewalks" was back-face culling (fixed 2026-08-30)**: reported from play of a terrain-following city — *"I'm afraid I'm only seeing very few sidewalks."* The obvious diagnosis, that terrain buries the 0.15 m block-floor slab, **was measured and is not what the player was seeing**. `GlThreeD` runs `GL_CULL_FACE` with `FrontFace(Ccw)`, and `builtin.tools.Triangulate.ToMesh` took ONE `v3Normal` doing two unrelated jobs — the plane LibTess sweeps in, and whether to write per-vertex normals — so `ExtrudePoly` passed `PairedNormals ? up : Vector3.Zero`, and a **zero normal makes LibTess derive the projection plane from the polygon itself**. For a ring that is no longer planar, which after §7e is every block on a slope, that derivation **flips**: measured over the block outlines, `Yelukhdidru`/3000 came out **234 up / 211 down on a 5.8 % plane** and 219/226 on rolling ground, against **437/8 flat** — so about half of a hillside city's pavements were culled away, with a complete mesh, no exception and nothing in the log. `GenerateClusterQuartersOperator` was the ONLY `ExtrudePoly` caller with a cap and no `PairedNormals`, so the one caller that said "no vertex normals" was the one caller that said "guess my plane". Naming the plane gives **445 of 445 upward in every case**. The plane and the vertex normal are separate arguments now and a **zero plane is refused** rather than falling back to the guess. **The parent investigation had already proved "445 of 445 blocks triangulate fully" — true, and the wrong question**; `TriangulateNonPlanarTests` concluded that naming the sweep normal "does not change the result" on the strength of the heights being equal, which they are. Whatever else you check about a triangulation, check which way it points. **Second half, same symptom**: block floors were emitted at `CreateFromMatMesh(mmmerged, 400f)` — shorter than a fragment's diagonal — against **100000 m for the road mesh** and 3000 m for the terrain, so pavements stopped at ~700–820 m (400 + the merged AABB's radius, median 302 m) while `PlayerViewer` keeps fragments to 1131 m: roughly **9 of 25 loaded fragments showed pavements, 25 showed roads**. `MaxDrawDistance` is derived from `PlayerViewer.LoadNSurroundingFragments` and cannot cull a fragment the loader kept; it costs 339 vertices at the worst fragment of the 3000 m city. **Burial IS real and is deliberately NOT fixed**: replicating `ClusterConformElevationOperator.Grade` on the real shipped terrain (`GroundOperator`'s diamond-square, whose gradient is **15.6–16.6 % over a 20 m cell at the median** and whose relief inside a 200 m window is **57–60 m**), **18–25 % of block AREA is under terrain, median burial 2.3–2.6 m — but only 3–7 % of the kerb rim, and no block anywhere is fully buried**. That needs the finer in-city elevation grid §2c already deferred, not a wider radius. **What the flat city loses, measured block by block**: exactly the caps that were backwards — 8 of 445, 1 of 82, 1 of 10, 1 of 3 — every other cap identical vertex for vertex and index for index; plus 94 of 200 rooftop `powerline` end caps, **every one of them from backwards to forwards and none the other way**. No vertex moves; a face that was culled is now drawn. **`_generateQuarterFloor` swallowed every exception into a `Trace`** (filtered off by default) — now `Error`, with a brace-counting source scan, because an 8-line window passed until a comment pushed the call past it. **`ExtrudePoly`'s ctor stopped resolving `engine.physics.API` from `I`** (moved into `BuildStaticPhys`) — that lookup is why `BuildGeom`, the geometry the class exists for, could not be called from a test at all. **Mutation survivors**: (1) widening `Fragment.PartitionContains` to be closed on both sides survived the whole-city block-coverage test — no generated block centre lands exactly on a 400 m boundary, so a rule that draws a block twice is invisible to any amount of real data; `TheFragmentsPartitionThePlane` asks the boundary directly. (2) hard-coding `Vector3.UnitY` as the cap plane survived every pavement test, since blocks extrude straight up; `ExtrudePolyCapTests` sweeps 24 non-vertical axes for it. Tests: `tests/JoyceCode.Tests/engine/streets/QuarterFloorFacingTests.cs`, `builtin/tools/ExtrudePolyCapTests.cs`, `builtin/tools/TriangulateNonPlanarTests.cs`. **Found and NOT fixed**: `GenerateClusterQuartersOperator` is the only quarter consumer that does not skip `IsInvalid()` quarters (it draws more, not fewer, and the baselines produce none); `Triangulate.ToConvexArrays` has the same latent projection guess but feeds convex-hull construction, which does not care about winding.
- ✅ **A block delimiter describes one edge; the default FLAT city moved (fixed 2026-08-30; §7e's first "found and NOT fixed")**: `QuarterGenerator` filled each `QuarterDelim` from **two different steps of the block trace** — `StartPoint` from the junction the step *arrived* at, `StreetPoint`/`Stroke` from the one it *left* — so a delimiter described two edges a street apart and which one a consumer got depended on which field it read. **Measured over the four baselines, 2936 of 2936 boundary segments**: the segment `delims[i].StartPoint → delims[i+1].StartPoint` is **0.00° off `delims[i+1].Stroke` at 4.9–8.9 m** (half a carriageway) and **60–76° off its own at 35–51 m**. `QuarterDelim` is now corner + the edge leaving it, all three `get; private set;` written by one `SetEdge`; **`CornerStreetPoint` folds back into `StreetPoint`** (it was §7e's workaround for exactly this) and the source scan that policed the two is replaced by the private setters plus a reflection test. **Which repair matters**: moving the *corner* back instead rotates the estate ring → ClipperOffset → building → shop fronts → shops, i.e. it moves buildings; moving the *labels* forward leaves the polygon **bit for bit identical** (the outline of all 2936 corners hashes the same before and after), so blocks, estates, buildings, shop fronts, shops, quarter AABBs, quarter floors and the kerb do not move. **THIS IS THE FIRST CHANGE THAT MOVES THE SHIPPED FLAT CITY'S CONTENT, and the "flat city is bit for bit unchanged" invariant no longer holds unqualified from here.** What moves: `Placer` + `car3.CharacterCreator.ChooseStreetPoint` pick the next junction round the block for a given draw — **100 % of delimiters, 75–106 m median, 136.5 m max** — as does `TaxiNpcSpawnerModule`; `citizen.SpawnOperator` keeps the *same* nearest street point (identical candidate set) but its delimiter index moves one back, so a force-spawned NPC starts its loop at the corner of the nearest junction instead of the next one along (66–89 m). What does NOT move: Placer-placed citizens (`QuarterDelimIndex` is the random draw, and the citizen path never reads `pod.StreetPoint`), and `GenerateHouseDescriptionsOperator` — ⚠️ **which is not a consumer at all: the entire file is inside `#if false` from line 1 and compiles to nothing** (corrected 2026-08-30; the ≤ 0.0002 m rotation-invariance measured for it is real but describes dead code). **Consumers examined and left**: `GenerateShopsOperator` was named as a consumer and **is not one** — `_hasPedestrianAccess` reads `StartPoint` only, so no shop front moves; `Placer`'s own intent is genuinely ambiguous (the candidate set is the same either way) and it moves as a consequence of the type being fixed, not as a correction — but its pod is now internally consistent. **Metric separation does not work here**: two arms of a junction can be nearly collinear (previous stroke 0.00° off the same segment) and a short street brings a neighbouring junction to 25.5 m of a corner whose own is 25.7 m away — so every discriminating assertion is on IDENTITY (section-point membership, `Assert.Same` on the junction) with distances/angles compared as medians only. Tests: `tests/JoyceCode.Tests/engine/streets/QuarterDelimTests.cs`, `builtin/tools/QuarterLoopRouteTests.cs`, plus `QuarterFloorTests.ThePadSitsOnTheCornersOfARealBlock` — added because the pad's per-corner pairing was fixture-only (that mutation failed 2 before it, 5 after). **Found and NOT fixed**: `SegmentNavigator.NavigatorBehave` writes `StreetPointId` from the OLD `StreetPoint` the line before overwriting it, so the id lags by one update; pre-existing and unread.
- ✅ **A pavement corner is filed under the junction it stands on (fixed 2026-08-30; §7e's second "found and NOT fixed")**: `GenerateNavMapOperator` grouped every block corner under `delim.StreetPoint.Id` so that pedestrian crossings could be drawn at each junction — and a `QuarterDelim` is an **edge**, so that is the junction at the far end. Measured over the corner lists: a filed corner sat **70.3–97.3 m from the junction it was filed under at the median, 134.9 m at the worst**; corrected to the corner's own junction it is **6.9–12.4 m at the median, 46.5 m at the worst** — half a carriageway. (That correction named `CornerStreetPoint`; the delimiter entry above then folded that field back into `StreetPoint`, so the line reads `delim.StreetPoint` today and is right for the first time.) **§7e's description of the consequence was wrong, and only measurement caught it: NOT ONE crossing lane moves**, in any of the four baseline cities (4 / 14 / 222 / 1411 lanes, zero added, zero removed). The crossing loop never reads the filed list to find its corners — for a junction with 3+ arms it asks `GetSectionPointByStroke` for the two section points flanking each arm and looks *those* up by position in the city-wide corner table. The list decides only **which junctions are considered**, a set that is provably unchanged (a block is a closed ring, so the junctions its delimiters leave and the junctions its corners stand on are the same set rotated by one), and feeds the **one-arm dead-end branch**, which never fires — dead-end junctions exist (8/18/43/121 across the four cities) but every block traced through one is discarded first. So it was a latent defect one dead-end block away from being read. **What DOES move in the default flat city is the emission ORDER**: the filing drives the crossing loop's iteration, hence the order lanes land in `NavClusterContent.Lanes` and in each junction's `StartingLanes`, which is what an A* breaks ties on. The dictionary is a `SortedDictionary` by junction id now (matching the car-lane junction table twelve lines above it), so the order stops depending on which block was traced first — **1408 of 1411 crossing lanes in the 3000 m city and 222 of 222 in the 1500 m city come out at a different list position**, over an identical set. TALE 200/200 across that. `FileCornerUnderItsJunction` is hoisted out of the operator for the reason `SidewalkJunctionFor` was, plus one more: the height and the filing must name the SAME junction for a corner, and two inline reads of a delimiter is how they came to differ. A source scan forbids **any** read of a delimiter's own `StreetPoint` under `builtin/modules/satnav` — re-inlining the filing fails only that scan, which is the survivor it exists for. Tests: `tests/JoyceCode.Tests/builtin/modules/satnav/PedestrianCrossingTests.cs`.
- ✅ **The satnav guideline lies on the road (fixed 2026-08-30; pre-existing, unrelated to the elevation work)**: reported from play — *"Navigation guideline is hovering in the air."* `ToSomewhere._onJunctions` built the ribbon quads at `nl.Start.Position`, which is `NavJunction.GroundHeight + MetaGen.ClusterNavigationHeight` — **3 m, the vehicle *hover* reference, not a surface** — and the parent transform then took a flat `0.5f * UnitY` off: ground + 2.5 against a road at ground + `CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE` = ground + 2.0. Half a metre, in the flat game too. The 0.5 was a z-fighting margin applied to the wrong base. **`builtin.modules.satnav.RouteRibbon` owns the arithmetic now**, starting from `GroundHeight` — which is why `NavJunction` carries the ground rather than a position with an offset baked in — and adding the offset for the surface the route is actually over: a **car** ribbon takes `CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE` (the term the road mesh and `JunctionCollider` use), a **pedestrian** ribbon takes `NavJunction.WalkingHeightOf`, one kerb higher, because the pavement is the block floor's extruded top face. The parent transform's blanket offset is gone. **The lift is 0.1 m, and the number comes from the depth buffer**: `Sdl3WindowBackend` asks SDL for a **16-bit** depth buffer and the play camera runs near = 1, far = √3·1000+100, so the quantum on a coplanar surface is about z²/65535 — 6 mm at 20 m, 38 mm at 50 m, 0.15 m at 100 m, 0.61 m at 200 m. No fixed lift keeps a long route off the road at its far end, so the choice is about the near end: 0.1 m holds to ~80 m and is a tenth of `HoverSurfaceProbe.SurfaceClearance` (1 m), so it cannot read as floating; beyond that the far end may shimmer, which is depth precision, not height. **The slope is free**: each end takes its own junction's surface height and the along-vector is their difference, so a ribbon over a climbing road climbs with it — asserted on all four corners. The 4 m quad is centred on the lane (`start + Width/2·right` extending `−Width·right`), confirmed rather than assumed since fix 7f changes which lane that is. Tests: `tests/JoyceCode.Tests/builtin/modules/satnav/RouteRibbonTests.cs`; `_onJunctions` needs a booted engine and is covered by a source scan. **Found and NOT fixed**: `Vector3.Normalize` of the plan direction is NaN for a lane with no horizontal extent — pre-existing, and the generator emits none.
- ✅ **The satnav guideline is routed on car lanes (fixed 2026-08-30; pre-existing, unrelated to the elevation work)**: reported from play — *"Navigation guideline ... is a bit off street."* `LocalPathfinder._transportType` defaulted to `TransportationType.Pedestrian` and `builtin/modules/satnav/Route.cs` named no type at all — not for the pathfinder, not for either `TryCreateCursor` — so the route the **player** follows in a hover ship was planned over the **sidewalk** lanes `GenerateNavMapOperator` traces round every block, and the ribbon, which is centred on whichever lane the route used, has always been drawn on the pavement. **The default is gone rather than corrected**: a default that is right for one of two networks is how this recurs, so `LocalPathfinder`, `NavCluster`/`NavClusterContent.TryCreateCursor`, `satnav.Module.CreateRoute` and `Route` all take the type as a required argument and `engine.quest.ToSomewhere.TransportType` is `required` — a new quest target cannot compile without saying. All four shipped sites (`Taxi` pickup + driving, `VisitAgentTwelve`, `HelloFishmonger`) sense the player through `MainPlayModule.PhysicsStem`, the hover ship's own physics name, so all four are `Car`. **Both halves need the SAME type**: `TryCreateCursor` returns `Nil` rather than a lane of the wrong kind, so a pedestrian cursor into a car A* finds no route at all rather than a poor one. **`builtin.modules.satnav.RoutePlan` exists because of the survivor**: `Route.Search` hops onto the logical thread and was exercised by nothing, so re-introducing the defect in its exact original location passed the whole suite — the two cursors, the A* and the truncation of the last lane now live in `RoutePlan`, which needs only a `NavCluster` and is tested end to end against a fixture with a car lane and a pavement lane on the same street; `Route` keeps the thread hopping and one scanned line. The truncation gained its first test on the way. Tests: `tests/JoyceCode.Tests/builtin/modules/satnav/SatnavRouteTransportTests.cs`. **Deliberately unchanged**: `TransportationTypeFlags()` still defaults to `Pedestrian` and `NavLane.AllowedTypes` still initialises through it — that is *what may use this lane*, a different question, and both engine emission sites pass explicitly. **Trap**: a new `.cs` under `JoyceCode/` must be added to `JoyceCode.projitems` by hand; the shared project lists files explicitly and does not glob.
- ✅ **The kerb meets the road AT ITS CORNERS; block corners take their own junction (fixed 2026-08-30; ⚠️ title corrected 2026-09-02 — between the corners it did NOT, see §7o above)**: reported from play of a terrain-following city — *"sidewalks still hover in the air and/or are in the ground."* There is no separate sidewalk geometry: `GenerateClusterQuartersOperator` extrudes the block polygon up by `MetaGen.QuarterSidewalkOffset` (0.15 m), so the **top face is the pavement, the sides are the kerb, and the block's outline IS the kerb line**. **The defect is a pairing, and it is pre-existing** (`QuarterGenerator` has always traced blocks this way): a `QuarterDelim` is an **edge**, and its two halves belong to different junctions — `StreetPoint` is the junction the edge *leaves* and `StartPoint` is a section point of the junction it *arrives at*. Everything that asked "how high is this corner" paired the two, so a corner took the height of a junction **70–97 m away at the median, 135 m at the worst** (against 7–12 m for its own), and **2936 of 2936 corners** over four generated cities are a section point of the *next* delimiter's junction and of no other. Measured over `seed000`/1500 m and `Yelukhdidru`/3000 m with relaxed heights: on a **1 % plane** the kerb ran −1.12…+1.47 m with the pavement **below** the road at 41–45 % of corners; at 5.8 %, −7.21…+7.86 m; on rolling hills, ±20.7 m. **Both engine sites were of that shape** — the pad's least-squares fit, and the sidewalk `NavJunction` added one commit earlier, which is where an NPC's feet go — so pavement and walker were wrong *together* and never disagreed. `QuarterDelim` gained **`CornerStreetPoint`**, written together with `StartPoint` by `SetCorner`, plus a source scan forbidding a height read from a delimiter's own `StreetPoint` — **both superseded the same day: the delimiter itself was straddling two edges, and once that was fixed `StreetPoint` IS the corner's junction (see the delimiter entry above).** **Fixing the pairing is not enough**: the pad is a *plane* through corners that are not coplanar even over an exactly planar hillside, leaving 0.02 m median / 0.36 m p99 / 1.66 m worst residual and still inverting 5 % of kerbs — ⚠️ **those figures are for an exactly planar 5.8 % slope and are wildly optimistic; on the shipped diamond-square terrain the same residual is median 0.00 m but p05 −3.5…−6.1 m, p99 ±7…10.5 m, worst −18.3 m / +16.9 m** (measured 2026-08-30). A block's corners span 10–16 m of height over a 66 m median edge; a *plane* cannot be close. Every consumer of `Quarter.GroundHeightAt` — buildings, trees, shop fronts, TALE doors, and the citizen loop walker — inherits that — so the floor's boundary now takes `Quarter.CornerGroundHeightAt` (the corner's own junction height, exactly) and the kerb is exactly `QuarterSidewalkOffset` everywhere. The outline is therefore non-planar; `TriangulateNonPlanarTests` already pins that LibTess keeps every vertex's height, and `ExtrudePoly.BuildStaticPhys` builds convex hulls. **The trade, measured not assumed**: `GroundHeightAt` (the pad) is still what buildings, trees, shop fronts and TALE doors stand on and is no longer exactly the floor — but the fit is parametrised about the corners' centroid, so **the plane at the centroid is the mean of the corner heights identically** (measured difference 0.0000 m over whole cities); the two part company at the kerb, where nothing stands, and coincide in the middle of the block, where everything does. §7c's "everything on a block agrees" is weakened exactly that far, and the doc now says so. **Flat city unchanged by two routes**: `GroundHeightAt` short-circuits on `IsFlat` before the fit, and `FlatStreetHeight` returns `AverageHeight` itself, so every corner still lands on `AverageHeight + ClusterStreetHeight` — asserted over whole generated cities with an average that has no exact binary form. `FloorOutlineOf` and `SidewalkJunctionFor` were hoisted out of their operators so the arithmetic is exercised; the mesh emission and `BuildStaticPhys` need a fragment and a physics world and are not. Tests: `tests/JoyceCode.Tests/engine/streets/QuarterFloorTests.cs`, `QuarterPadTests`. **Found and NOT fixed at the time, both fixed the same day** — see the two entries above, where measurement corrected both descriptions: `GenerateShopsOperator` was not a consumer of the off-by-one at all, and no crossing lane was actually drawn between the wrong corners.
- ✅ **NPCs walk at street height, off the nav lanes (fixed 2026-08-30)**: reported from play with the design steer — *"NPCs do not seem to honour street height. I believe we do not want to afford raycasting for NPCs, so we need a linear function for their elevation based on street nodes."* **That function already existed and was being discarded.** `GenerateNavMapOperator` gives every car-lane `NavJunction` the height of the `StreetPoint` it IS (relaxed street height, cut and fill included) and every sidewalk junction the height of its delimiter's junction; lanes measure with `Vector3.Distance` and split with `Vector3.Lerp`, so interpolation along a lane is **already linear in the street nodes, with no raycast and no per-NPC cost**. `nogameCode/nogame/characters/citizen/StreetRouteBuilder.cs` then gave every waypoint ONE Y, computed once at the route's start and from `ClusterDesc.GroundHeightAt` (the **terrain**), so a route across a hill came out flat. **The offsets are the trap**: `NavJunction.Position.Y` is ground + `ClusterNavigationHeight` (3 m, the *vehicle* hover reference) while a walker wants ground + `ClusterStreetHeight` (2 m) + `QuarterSidewalkOffset` (0.15 m) — so `NavJunction` now carries **`GroundHeight`** and each consumer adds its own offset via `NavigationHeightOf`/`WalkingHeightOf`; subtracting one constant to add another is how two heights drift apart with nothing failing. **Every junction in the engine is built by `NavJunction.At` / `Between` / `AtNavigationHeight`**, enforced by a source scan, because an object initialiser sets whichever half its author was thinking of and leaves the other at zero — right position, right route, every NPC crossing it dropped to 2.15 m above sea level. Both engine sites were of that shape. **Flat-city arithmetic**: lane Y is `AverageHeight + 3`, the old route height was `AverageHeight + 2.15`, and `WalkingHeightOf(AverageHeight)` reproduces it exactly — pinned on the term, not just the value. **`builtin.modules.satnav.PedestrianRoute.WaypointFor` lives in Joyce because `StreetRouteBuilder` does not**: the test assembly references `Joyce`, never `nogameCode`, so the piece that decides a waypoint (the lane end's own walking height, 1.5 m right onto the sidewalk) moved where a hand-built chain of lanes over a hill can walk it; a source scan stands in for the call site. The two ends of a route are still terrain samples — nobody stands on a junction — but the **destination now samples at the destination**, as does `GoToStrategyPart`'s straight-line fallback. Other consumers: `NavMeshRouteGenerator` only forwards; `TaleEntityStrategy`/`TaleWalkBehavior`/`WalkBehavior` never touch a waypoint's Y (`SegmentNavigator` lerps in 3D); `QuarterLoopRouteGenerator` was already per-waypoint correct since §7c; `Route.cs`'s truncation junction now goes through the factory. **Found and NOT changed** — ⚠️ **and this description was WRONG; corrected 2026-08-30**: the claim was that `SpatialModel._computeStreetEntryCandidates` carries the *vehicle* clearance, 0.85 m high. It does not. That function takes the lane endpoints' **XZ only** and overwrites Y with `junctionCenter.Y` (`SpatialModel.cs:443-448`), which is the correct walking height and has been since the original 2026-08-01 commit `b62c5f2f`. **Street entry candidates are correct today.** The 0.85 m defect is real but lives in **`_snapToPedestrianLane` (`SpatialModel.cs:357-402`)**, which returns a point on the lane in full 3-D and is used for **building and shop `EntryPosition`** (lines 197, 245) — i.e. shop and home doors, not street corners. Fixing that one moves NPCs standing at doors in the flat city by 0.85 m. Tests: `tests/JoyceCode.Tests/builtin/modules/satnav/NavJunctionHeightTests.cs`. `GenerateNavMapOperator` itself needs a stroke store, a quarter store and the container and is not exercised — dropping `streetPoint.LevelElevation` from a car junction survives, tolerable only because no shipped ruleset raises a junction.
- ✅ **The junction cap has a collider now (fixed 2026-08-30)**: reported from play — *"the car still is a bit stuck on the surface BETWEEN the individual branches of a junction"* — and it is the gap §7b recorded as a known limit. `_generateJunction` fills the area between the branches with a fan over `StreetPoint.GetSectionArray()`, and that fan had geometry and **no collider at all**; `engine.streets.generation.JunctionCollider` now gives it one, emitted under the same two conditions and the same `IsInsideLocal` ownership rule as `DeckCollider` (so a flat city, whose fragment floor plane already covers every junction on the ground, emits nothing — asserted over whole generated cities, not a fixture). **The obvious diagnosis is only half of it, and the halves were measured rather than assumed**: the fraction of a cap covered by NO stroke box is **0.1 % at the median** (three boxes converging on one point do overlap across most of a junction), though a third of junctions have some and the worst junction of the 3000 m city has 92 m² standing on nothing. The bigger half is that `_shearOntoSlope` holds the road MESH flat over each junction footprint while `DeckCollider` tilts across the stroke's whole length — so inside a junction the collider climbs while the picture is level, and two branches of different slope cross there. The cap reaches **7.6–10.6 m into its own strokes' boxes at the median, 28.4 m at the worst**, and `GradePolicy` allows 5–14 %: **0.4 m to 4 m of invisible step**. A flat slab at the junction's one height wins wherever a box has climbed above the road. **The shape is the cap, not a disc over it**: measured, the circumscribed disc is 2.75× the cap's plan area at the median, 5.3× at p99 and **34× at the worst junction** (two nearly collinear strokes push a section point 42 m out and the disc becomes an 85 m pancake) — an invisible apron onto ground the terrain-following city may have put well below, which is the artefact being removed, not introduced. So it is a Bepu `ConvexHull` over the cap's own corners, top face on the road. **Fewer than three section points means no cap** (a two-arm fan is two degenerate triangles; the two strokes' boxes overlap across the junction anyway). **`JunctionCollider.SurfaceHeightOf` exists because of the one surviving mutation**: reading `AverageHeight` at the call site compiles, leaves the flat city bit-for-bit identical and floats a pancake at the mean height over every junction of a terrain-following one — and `ClusterGroundHeightTests` cannot catch it, since the operator is already on that allow-list for its floor plane and the list is per file. The test now compares the slab against the cap MESH (a separate expression in `_generateJunction`) over a sloping source with every other junction raised onto a deck. Tests: `tests/JoyceCode.Tests/engine/streets/JunctionColliderTests.cs`; `StreetFragmentOwnershipTests` extended to per-junction loops. The Bepu emission itself needs a booted engine and is not exercised. Deliberately not fixed: `DeckCollider` still tilts across the junction footprints instead of flattening over them as the mesh does, and its own height expression is still inline.
- ✅ **The ground under a terrain-following city now agrees with the roads on it (§2c, 2026-08-29)**: with `joyce.DisableClusterFlattening=true` the streets were relaxed to buildable gradients while the terrain was left exactly as the noise made it, so a road sliced through the hillside it crossed and stood proud of the dip it spanned. `engine.elevation.ClusterConformElevationOperator` grades the city site toward the street height field. **The ordering cycle dissolves through the existing layer mechanism, not new machinery**: elevation operators register at ordered layer strings and each reads the layers strictly *below* its own, so the stack is `/000002/fillGrid` → `/000100/flattenCluster` → **`/000150/conformCluster`** → `/000200/intercityTrails`, and `TerrainStreetHeight` samples `ClusterConformElevationOperator.Layer` (= `/000150`) instead of `TOP_LAYER` — `Loader.GetHeightAt` already took the parameter. Streets therefore read unconformed ground, the conforming pass reads the street graph, and everything else (rendering, `GetWalkingHeightAt`, the hover probe's terrain fallback) still reads `TOP_LAYER` and sees the result. **Below intercity on purpose**: `IntercityTrackElevationOperator` hard-sets an absolute height along a narrow band, which a city may not smooth away, and keeping it last leaves its relationship to the city terrain exactly what it was. **§2c's "flatten a corridor" wording was wrong and has been corrected in the doc**: `GroundResolution = 20` over `FragmentSize = 400` is **one elevation sample every 20 m** while a street is 8–22 m wide, so a corridor is about ONE CELL and cutting it terraces rather than cuts — a real cutting needs a finer grid inside cities and is explicitly a later, larger change. What is written instead: a smoothstep kernel (`t²(3−2t)`, flat at *both* ends so the graded ground neither creases at the radius nor tents over the road) with a radius of **three cells = 60 m**, a **weighted mean** of the streets in range (nearest-wins puts a seam down the middle of every block), and an influence equal to the **largest single weight** (a sum would need clamping, and a clamp is a hard edge). Height only — `Biome = 1` from the flattening operator below still means "this is city". Deterministic: strokes iterated by `Sid` as `GradeRelaxer` does, and the field is built from the WHOLE stroke graph, so neighbouring fragments cannot disagree along a shared edge. Cost is one AABB reject per stroke per sample: 441 × strokes per fragment, a few ms for the largest city, once, at generation time. **The flat default path is proved untouched twice**: the operator is not registered at all unless the flag is on, and it short-circuits on `IStreetHeightSource.IsFlat` *before* touching `StrokeStore()`. It also checks its own AABB, because `Cache.ElevationCacheGetAt` runs the TOP layer's operator for every fragment in the world with the intersection test disabled — and this is the top layer wherever no intercity network is registered above it. Found and NOT fixed: `ClusterBaseElevationOperator` and `IntercityTrackElevationOperator` divide the segment span by the sample COUNT rather than count−1, placing every sample ~5 % short and restarting the error each fragment (harmless for an operator writing a constant inside a rectangle; fixing it would move the flat baseline). Tests: `tests/JoyceCode.Tests/engine/streets/StreetHeightFieldTests.cs` and `tests/JoyceCode.Tests/engine/elevation/ClusterConformElevationOperatorTests.cs`; the operator on a terrain-following city needs the `I` container, the engine and the elevation cache and is not exercised.
- ✅ **The hover ship asks the physics world what it is over (fixed 2026-08-29)**: follow-up to the friction/servo fix below, which made contact benign without removing its cause — the ship was still *commanded* below the surface it stood on, because `HoverController` aimed at `Loader.GetNavigationHeightAt` = **terrain** + `ClusterNavigationHeight`, while what it drives on is a **street collider** at relaxed-street + `CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE`. `HoverController._probeSurfaceBelow` now does what `WalkController` has always done for the walking player: `aPhysics.RayCastSync(origin, -Vector3.UnitY, …)`, origin 2 m **above** the ship (a ray starting inside the deck you stand on begins inside a convex and reports nothing), 24 m deep, rejecting the caster. `engine.physics.API.RayCastSync` **takes the simulation lock itself** — do not add another. **The ray is an ADDITION, not a replacement**: terrain has no collider at all (`Fragment._createGround` adds no physics), so `engine.physics.HoverSurfaceProbe.HoverTargetHeight` combines the two with `max` — a built surface may raise the target, never lower it, which is also what keeps the ship out of the walls of a road cutting. **The clearance is derived so a flat city is bit-for-bit unchanged**: every drivable surface there (fragment floor plane, quarter floors, level deck colliders) tops out at `AverageHeight + CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE` and the old target is `AverageHeight + ClusterNavigationHeight`, so `SurfaceClearance = ClusterNavigationHeight - CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE` (1 m) reproduces it exactly and `max` is a no-op — reusing `ClusterNavigationHeight` as the clearance, the obvious guess, would raise the whole default city by 2 m. **The ray hovers over the world, not the traffic in it**: `HoverSurfaceProbe.IsHoverSurface` rejects a body whose `SolidLayerMask` is *only* moving-kind layers (`Player`/`Npc`/weapons/`Collectable`/`QuestMarker`) — deliberately not a plain intersection, since a house declares no mask and keeps `Layers.All` — because the *climb* side of the servo keeps full authority and a pedestrian passing under a parked ship would launch it; an unlabelled collider counts only when `Static`/`Kinematic`. The terrain sample still owns the one-sided shove and the rescue (both test `heightAtTarget`, not the probed target: those are about being *inside the hill*). Tests: `tests/JoyceCode.Tests/engine/physics/HoverSurfaceProbeTests.cs`; the cast itself needs a running simulation and is covered only by a source scan. **Deliberately unchanged**: still one ray at the ship's centre, and the tilt response still reads the terrain ahead.
- ✅ **Friction is per body now, and the hover ship stops being glued to what it touches (fixed 2026-08-29)**: `NarrowPhaseCallbacks.ConfigureContactManifold` set `pairMaterial.FrictionCoefficient = 1f` for **every pair in the world**, under a comment inherited from the Bepu demo it was copied from. 1.0 is a wheels-on-asphalt number and the player's ship is a hover vehicle with nothing to grip, and the numbers cross: pressed onto a surface at the hover loop's constant descent authority it carried a normal load of 500 kg × 16 m/s² = **8000 N** against a maximum thrust of 500 kg × `LinearThrust` × 255/256 = **7470 N** — friction beat the engine, so the car was immobile, not merely sticky. `CollisionProperties.Friction` is per body now, **defaults to `DefaultFriction` = 1.0 so nothing else in the world changes**, and a pair takes the LOWER of the two (`CombineFriction`) so a body that declares itself slippery is slippery against everything; the ship declares 0.05 via `HoverModule.FrictionShip`. What put it in contact at all is a signed disagreement between two height references: the hover loop aims at `ClusterDesc.GroundHeightAt` + `ClusterNavigationHeight` (terrain + 3 m) while the road is `StreetHeightSource.GroundHeightAt(junction)` + `CLUSTER_STREET_ABOVE_CLUSTER_AVERAGE` (relaxed street + 2 m), and `GradeRelaxer` removes only the excess gradient — so on a hillside the road is cut at one end and **filled** at the other, and wherever the fill exceeds 1 m the road stands above what the hover loop aims at and the loop commands a descent *into* it. `engine.physics.HoverHeightServo` makes that command asymmetric: **climbing keeps full authority** (the terrain has no collider, so being slow to rise is being inside the hill) while **descending is proportional** to the height error and saturates at `LevelDownThrust` only beyond 2.4 m — which also ends the 13 cm undershoot the constant command produced on every arrival. **Deliberately not fixed**: the height reference itself (a positional road query would need thread-safe `StrokeStore` machinery — `GetClosestStroke`/`GetClosestPoint` take a junction and share `_tmp*` scratch buffers — and the corridor-conforming pass in `docs/roadmap/proposed/STREETS-3D-TOPOLOGY.md` §2c removes the discrepancy at its source), and the ship's **zero-length collision cylinder**, which is relevant here (a flat disc rests with its whole face, the most friction leverage a shape can offer) but whose long-standing `HoverModule` comment still holds. Tests: `tests/JoyceCode.Tests/engine/physics/{HoverHeightServoTests,PairFrictionTests}.cs`; the closed-loop test integrates every term in `HoverController` that touches vertical velocity, since each is a constant with no dependency on the world.
- ✅ **Dooming an entity twice is legal now (fixed 2026-08-29)**: a player crashed with `ArgumentException: Entity Entity 1:1605.0 already was doomed before` from `AutoRemoveBehavior.Behave` → `Engine.AddDoomedEntity`. **Dooming is not destroying**: `Engine._executeDoomedEntities` runs only when `_isCompiling || (_frameNumber & 7) != 0` **and** `timeLeft > 0.005`, so on every eighth frame (the one that instead calls `WorldLoaderProvideFragments`) and on any frame whose budget is spent, the drain does not run at all — the entity stays alive, stays in every `AEntitySetSystem` query, and `BehaviorSystem` ticks it again. `AutoRemoveBehavior` was the **only** self-removing behaviour in the tree with no latch (`CubeVanishBehavior`, `PolytopeVanishBehaviour`, `FollowQuestToastBehavior` all carry a `bool _isDoomed`), so it re-doomed on the very next tick. Not a rare race: ~1 in 8 quest-completion toasts. **The doom set is now idempotent** rather than the callers being made to coordinate, because two owners genuinely cannot see each other — `Fragment.RemoveFragmentEntities` dooms everything its fragment owns while a behaviour on one of those dooms itself, and `nogame.inv.coin.Behavior` dooms the coin from `OnCollision`, where two contacts in one frame are ordinary physics. What must hold instead is *destroyed exactly once*, and that was **broken in Release**: the `#if !DEBUG` path kept an undeduplicated list of lists, and `Entity.Dispose` checks neither `IsAlive` nor `Version` while `World.On(EntityDisposedMessage)` calls `ReleaseInt` — a second Dispose releases the id twice and the dispenser then hands one id to two live entities. New `engine.DoomedEntitySet` (`JoyceCode/engine/DoomedEntitySet.cs`) replaces both `#if` branches with one locked `HashSet`: duplicate → `Trace(Dc.Engine, …)` and return (a Warning that fires during normal play is a Warning nobody reads); **not alive** → still `ErrorThrow` in DEBUG, `Error` + refuse in Release, because that is a stale struct whose id may now denote somebody else. `AddDoomedEntities` also stops storing the caller's `IList` by reference. Tests: `tests/JoyceCode.Tests/engine/DoomedEntitySetTests.cs` (drives a bare `DefaultEcs.World` — constructing an `Engine` registers ~15 services into the global `I` container and would collide with whatever test class registered first) and the drift test `tests/JoyceCode.Tests/engine/behave/SelfDoomingBehaviorTests.cs`, which brace-scans every `Behave` body for `AddDoomedEntity(entity)` and fails if that class has no latch field. Mutation tested: restoring the duplicate throw fails 3, dropping the `IsAlive` guard fails 1, removing the latch fails 1. **Deliberately not changed: the drain schedule.** Skipping it is budgeting, not a bug; the invariant that had to hold is idempotent doom + destroy-once, not immediate destruction.
- ✅ **The taxi passenger stood in a T-pose at a street point (fixed 2026-08-25; the missing guard was written 2026-08-31 — see §7m above)**: `quests/Taxi/DrivingStrategy._spawnWaitingPerson` built its character through `EntityCreator` with **no `EntityStrategyFactory`, no `BehaviorFactory` and no `InitialAnimName`** — the only one of the five character-creation sites in the tree with none of the three. Nothing ever called `SetAnimation`, so `AnimationState.ModelAnimation` stayed null, `CameraOutput._appendInstanceNoLock` substituted `NullAnimationsEntry`, `GlThreeD.DrawMeshInstanced` set `vertexFlags = 4`, and `LIghtingVS.vert`'s `if (iVertexFlags < 4)` skipped skinning entirely — i.e. the mesh as authored, the bind pose. The quest destination is placed with `PlacementDescription.Reference.StreetPoint` and `GetWalkingPosAt` only adjusts Y, which is why it presented as *a citizen T-posing at a street point on the sidewalk*, one per taxi quest, for the whole driving phase. **It survived the two previous T-pose fixes (`ca8106ba`, `44f8c2f3`) because both hardened BEHAVIOURS**: the retries and every line of `StuckAnimationReporter` live inside a behaviour, and this entity has no `Behavior` component at all, so nothing watched it and nothing logged. Fix is `InitialAnimName = cmd.IdleAnimName`; **`IdleBehavior` is not an option here** — its `OnAttach` reads the `Body` component and this entity has no physics object (`CollisionPropertiesFactory` is null), so DefaultEcs would hand it a reference into unused storage. `EntityCreator` now **checks that one-shot's return value** and Errors with `AnimationState.DescribeFailure` when it refuses: for the two callers that pass only an `InitialAnimName` (this one and the niceday NPCs) it is the sole attempt that will ever be made, and an unchecked one-shot exactly like it is what made fix #106 a no-op twice. ⚠️ **CORRECTION (2026-08-30): the drift test described here was never written.** This entry claimed `tests/JoyceCode.Tests/engine/joyce/CharacterAnimationDriverTests.cs` scans nogameCode for `EntityCreator` initializers; that file existed in **no commit on any branch**. The *fix* landed, the guard did not. Worse, the criterion as stated ("names one of the three drivers") **would not have caught the remaining hole anyway**: all six `EntityCreator` sites name a driver, but `nogame.npcs.niceday` NPCs start in `RestStrategy`, whose `NearbyBehavior` only drove the "E to Talk" prompt and **never called `SetAnimation`** — so their entire animation was the unretried `InitialAnimName` one-shot, the exact shape that made fix #106 a no-op twice. ✅ **Written 2026-08-31 with §7m, and NOT to that criterion**: it asserts that a creation site can REACH a class that calls `SetAnimation`, and separately that every behaviour a character's strategy can rest it in does so — the second because the first survives gutting `IdleBehavior`, `WalkBehavior` being in the same closure. Still open, deliberately: `DrivingStrategy.OnExit` only deletes the passenger when `_hasWaitingPerson` is set, and that flag is set inside the queued setup action of an `async void` spawn, so an early exit leaks the entity until its fragment unloads.
- ✅ **The Silk.NET exit is COMPLETE — Platform Phases 0–5 all merged (2026-08-14)**: windowing/input/audio moved to SDL3 and hand-written P/Invoke (Phases 2–3), model import became a build step (Phase 4), and the GL binding is now generated from the Khronos `gl.xml` registry (Phase 5). **Zero Silk.NET references remain in any shipping project**; what survives is `Silk.NET.Assimp` in **`JoyceFbx`** (build-time only) and `Silk.NET.OpenGL` in the WP-5.0/5.1 comparison tools, which exist to diff our binding *against* Silk. Renamed by WP-5.4: **`Splash.OpenGL`** (the GL backend, was `Splash.Silk`) and **`Splash.API.OpenGL`** (the generated binding, was `Splash.GL`, namespace was `Karawan.Graphics.OpenGL`). **Naming rule that generalises: `Karawan` and `Wuka` are startup *applications*, not layers — shared code belongs to its subsystem's namespace** (`Splash`, `Boom`), never to whichever app happens to start it. Full state, including what is still open (GATE-C Linux has *never* been run; no CI exists at all — KI-17), in `docs/roadmap/proposed/PLATFORM-BACKEND-STATUS.md`.
- ⚠️ **Build tools run from their PUBLISHED output, and `dotnet build` does not refresh them.** After changing or pulling `Chushi/`, `Mazu/` or `Tooling/Cmdline/`, run `bash Chushi/build.sh` and/or `bash Tooling/Cmdline/build.sh` **before** building `nogame`. A stale published Chushi performs no bake at all — no error, no warning, just no output files — which is how a build shipped in which no character model loaded, surfacing three layers away as *"No fbx importer is available for url …"*. `res2target` now **fails the build** when a declared `mo-`/`ac-`/`sc-` artifact is missing, naming the file and this command. Detail in `docs/main/docs/build/PIPELINE.md`.
- ✅ **Input bindings are actions now, stored by key POSITION (WP-6.4, 2026-08-15)**: `builtin.controllers.bindings` holds the layer — `Control` (a physical control with a canonical string form like `Key:W` / `GamepadButton:DPadUp` / `GamepadStick:0`), `BindingTable` (live, lock-guarded, JSON round-trip, capture mode for rebinding), and `IInputModifier` (`deadzone`, `curve`, `invert`, `range`). **All** shipped bindings live in **`models/nogame.bindings.json`**, declared as a resource; user rebindings persist to `bindings.json` under `Engine.RWPath` and are layered per-action over the defaults, so bindings added by an update still appear. **Store `ScanCode`, never the key string**: `"w"` is a position that merely looks like a character (on AZERTY that key prints Z), and a rebinding UI must ask the platform for the display label separately. `InputMapper` turns a control into a logical EVENT (`<interact>`) and consults the table first, falling through to the legacy flat `MapButtonToLogical` (retained as public API but **empty in the shipped config**); `InputController` turns one into polled analog STATE and reads the same table — `walkforward`/`walkbackward`/`walkleft`/`walkright`/`run` (was `case "w":`…), `move`/`look` on `GamepadStick:0`/`:1` (was `switch (ev.Data1)`), `brake`/`accelerate` on `GamepadTrigger:0`/`:1`. **`StickTransfer` is deleted** — its `sign(x)·|x⁴|` is `"curve 4"` in the JSON, and `_onTriggerMoved`'s `(x+1)/2*255` is `"range -1 1 0 255"`. **Nothing is duplicated as a fallback** in either place: a second copy of the defaults is exactly how a rebind leaves the old control still firing. If the file goes missing the player cannot move, so `InputController.OnModuleActivate` logs an Error naming the unbound actions; the drift test iterates `InputController.RequiredActions` itself, not a copy. Two traps: the accumulator branches in `_onStickMoved` must test the **post-modifier** value (raw works only while the curve preserves sign — `invert` does not), and the stick/trigger **indices are contract** with `Sdl3WindowBackend._onGamepadAxis`, so renumbering there silently swaps two pedals without failing to compile. Still open: the `[HUMAN]` rebinding UI, and a GATE-C gamepad run.
- ⚠️ **Never touch an ECS entity in a continuation after an `await` (fixed 2026-08-14)**: this engine has no synchronization context, so a continuation resumes on a **thread-pool** thread, and the entity may have been detached meanwhile (`OnDetach` sets `_entity = default`; TALE NPCs are depopulated whenever the player leaves their cluster). `Entity.Set` then throws **"Entity was not created from a World"** — which reads like a thread-affinity error but is not: that text comes from `ThrowIf(WorldId == 0, …)`, i.e. a *default* Entity. DefaultEcs's actual thread check is a separate `[Conditional("DEBUG")]` assert with a different message. Use `AOneOfStrategy.TriggerStrategyOnLogicalThread`, which hops back via `Engine.RunMainThread` (inline when already on the logical thread) and re-checks `IsAlive` **after** the hop, since the detach can land in between. Related: **`async void` turns any escaping fault into a process kill** rather than a log line — that is why a routine "this NPC went away mid-await" presented as a crash. Fire-and-forget entry points must wrap and log.
- ✅ **Models are baked; FBX import is build-time only (Platform Phase 4, 2026-08-14)**: Chushi writes one `mo-{hash}` per declared model and the game deserialises that, so **`libassimp.so` is no longer in the APK** and no runtime project references Assimp. The importer moved to a new **`JoyceFbx`** project referenced by `Mazu`/`Chushi` and the tests only — putting it back in `Joyce` would re-add Assimp to every shipped target transitively, which is what used to happen. `ModelCache.FbxLoader` is the seam Chushi installs; in the game it is null and an unbaked fbx raises instead of silently working. Models are declared `"type": "model"` in `models/nogame.resources.json` **with their load properties** (`"modelProperties": { "Scale": "1" }`) because those are part of the bake identity — significance is decided by exclusion, so an unknown property forces a re-bake rather than silently reusing a stale file. The 12 animation-source fbx are `"type": "animationSource"` and ship nothing; their output is the existing `ac-{hash}`. **Bone order is the trap** (`AllBakedMatrices` is indexed `frame * NBones + boneIndex`; a reorder renders a foreign pose without crashing) — asserted per bone in `tests/JoyceCode.Tests/engine/joyce/BakedModelEquivalenceTests.cs`, which loads each model both ways and compares. Hash derivation is duplicated in `Tooling/Cmdline/GameConfig.ModelFileName` — **change both together**. Details in `docs/main/docs/build/PIPELINE.md` § *Models are baked*.
- ✅ **TALE storylets now ship and load on Android / installed builds (fixed 2026-08-03)**: `StoryletLibrary.LoadFromDirectory` enumerated `models/tale/*.json` off the filesystem, so nothing declared those 14 files as resources and neither `AndroidResources.xml` nor `InnoResources.iss` carried a single one (the 11 `tale/conversations/*.json` narration scripts *are* packaged — `nogame.narration.json` `__include__`s them, which is a different mechanism). On Android the model tree only exists inside the APK, so `Directory.Exists("./tale")` was false, `TaleModule.OnModuleActivate` returned before creating the manager, and `TaleModule.TaleManager` stayed **null while the module still reported itself active** — `Scene.cs` handed that null to `TaleSpawnOperator`, which crashed at the first per-fragment spawn poll (`_ensureClusterPopulated`). Fix: the 14 storylets are declared in `models/nogame.resources.json` with `"type": "taleStorylet"`; `TaleModule._collectStoryletTags` filters `/resources/list` on that same type to learn what to open and `StoryletLibrary.LoadFromAssets` reads them via `engine.Assets.Open`, so what ships and what loads are one declaration. Verified end-to-end on desktop (96 storylets from 14 resources) and in the staged APK assets. **`Directory`-based loading is now build-tool/test-harness only** (`ScenarioCompiler`, `Testbed`, `TestRunner`); game-runtime code must go through `engine.Assets`. Note `Wuka.csproj` `<Import>`s the manifest at project-evaluation time, so adding an asset needs **two** builds before it stages. Guards added so this can never present as a null-deref again: `TaleModule` logs an `Error` instead of a `Trace` when it disables itself, `Scene.cs` skips the spawn operator when `TaleManager` is null, `TaleSpawnOperator`'s ctor throws `ArgumentNullException`. Drift tests: `tests/JoyceCode.Tests/engine/tale/TaleStoryletResourceTests.cs`; details in `docs/main/docs/build/PIPELINE.md`.
- ✅ **TALE NPCs no longer stand in the middle of junctions (fixed 2026-08-01)**: `SpatialModel.ExtractFrom` creates one `street_segment` Location per `StreetPoint` — i.e. per **junction node**, positioned at the junction center. 26 storylets use street locations (`random_street`/`street_segment`), and drifter/socialite/hustler roles even get homes/workplaces assigned there, so NPCs walked to and idled at junction centers for whole schedule blocks. The old `_snapToPedestrianLane` mitigation projected the center onto the nearest pedestrian NavLane — at a junction that's almost always a **crossing lane**, whose interior is also in the roadway; with no NavCluster available the raw center was used. Fix: street-point locations now compute their standing points via `SpatialModel._computeStreetEntryCandidates` — ALL distinct pedestrian lane **endpoints** (NavJunctions = sidewalk corners) around the junction (nearest + 15 m tolerance, max 6, deduped on 0.5 m grid), never a lane-interior projection; without nav data, fall back to `StreetPoint.GetSectionArray()` (geometric sidewalk corners, already offset by half street width) each nudged 1 m outward. `Location.EntryCandidates` holds the corners; `Location.EntryPositionFor(npcId)` distributes NPCs bound to the same junction across them (used by `NpcSchedule.PositionAt`, `TaleEntityStrategy` travel destinations, `TaleSpawnOperator` transit sampling) so crowds don't stack on one corner. `Location.Position` stays at the junction center (route graph/travel-time metrics unchanged) — only the physical standing points move. Building/shop entry snapping unchanged, but all lane scans now go through `NavClusterContent.GetLanesNear` (octree-backed after `Recompile()`, full-list fallback before). `ValidateReachability` uses the endpoint-based check for `street_segment` locations so a crossing interior near a geometric corner can't flip unreachable→reachable. `Joyce.csproj` now declares `InternalsVisibleTo("JoyceCode.Tests")`; regression tests in `tests/JoyceCode.Tests/engine/tale/StreetEntryPositionTests.cs`.
- ✅ **Android build: missing `ac-{hash}` assets (fixed 2026-08-02)**: `Tooling/Cmdline/GameConfig.LoadAnimation` still read the pre-packs flat `animationUrls` field, so the generated `AndroidResources.xml` / `InnoResources.iss` listed one `ac-` entry per **model** (hashed from the model file name alone) while Chushi bakes one per **(model, pack)** pair — 12 phantom names vs. 14 real files, disjoint sets. Desktop shrugged it off (re-bakes on demand); the Wuka build failed with `Quelldatei "../nogame/generated/ac-…~" wurde nicht gefunden` for every entry. `LoadAnimation` now expands `packs` (returns `List<Resource>`, legacy `animationUrls` still supported), mirroring `AAssetImplementation._whenLoadedAnimations`. Generated-name derivation is duplicated between `Tooling/Cmdline` and `JoyceCode` on purpose (no project reference possible) — **change both together**; see `docs/main/docs/build/PIPELINE.md` for the verification one-liner.
- ✅ **Skeletal animation frame indexing (fixed 2026-07-20)**: `AnimationBatch.FrameNos` now always carries the **global** baked frame number (`ModelAnimation.GetGlobalFrame` = `FirstFrame + clamped local frame`), never the frame counted within its own clip. This is the contract every renderer strategy depends on: the SSBO shader consumes it directly as the `instanceFrameno` vertex attribute (`models/shaders/LIghtingVS.vert:136` computes `instanceFrameno * nBones + boneId` with no `FirstFrame` term), while the UBO/uniform paths slice `AllBakedMatrices` with `FrameNos[0]`. Previously `MeshBatch.Add` stored the local frame, so under the SSBO strategy — the live one on OpenGL ≥ 4.3 — **every clip read from the start of `AllBakedMatrices`**, which holds whichever animation sorts first by name (`Death_FallForwards` in all current packs). Symptom: walking characters played the fall animation; idle alternated between falling and standing as its frame counter crossed out of `Death`'s range. Note `BakeAnimations` assigns `FirstFrame` while iterating a `SortedDictionary`, so the physical layout is **alphabetical**, not the URL order in `nogame.animations.json`. Related: `AnimBatching` now follows `IThreeD.HasPerInstanceAnimationFrames` rather than the graphics API (macOS runs GL 4.1 → UBO strategy → needs batches split per animation+frame, despite being "OpenGL"); `ModelAnimationCollection.ValidateBakedLayout` asserts offsets tile the matrix array on every model load; `tests/JoyceCode.Tests/engine/joyce/BakedAnimationLayoutTests.cs` asserts the same for the baked `ac-*` files on disk.
- ✅ **Parallel Test Runner (2026-07-14)**: `./run_tests_parallel.sh` runs the TALE suite across all CPU cores (standard tier ~5 min → ~28 s, identical pass sets). Same filters as `run_tests.sh`; knobs: `JOYCE_TEST_JOBS`, `TEST_TIMEOUT`, `JOYCE_TEST_TIMEOUT_SCALE`, `JOYCE_TEST_SLEEP_SCALE` (in-script sleeps default to 0.1×), `JOYCE_TURBO=1` (opt-in engine free-run, fixed dt=1/60). TestRunner now waits on `TestDriverModule.SessionReady`/`Completed` events instead of fixed sleeps. See `docs/testing/plans/done/TIERS.md` § Parallel Runner. **Build TestRunner in Release first** (`dotnet build TestRunner/TestRunner.csproj -c Release`) — `dotnet build Karawan.sln` does not produce it. All three scripts now glob `TestRunner/bin/Release/*/TestRunner.dll` rather than hardcoding a TFM; `run_tests_parallel.sh` and `tools/run_one_tale_test.sh` lacked that fallback and had been silently unrunnable since the .NET 10 retarget (fixed 2026-08-15).
- ✅ **Animation Packs System**: Callers now request animations by pack name ("locomotion", "full", "locomotion_hardday") rather than hardcoding animation URLs. Pack registry (`AnimationPackRegistry`) resolves pack names to URLs at runtime. Enables cache sharing: player and citizen NPCs using the same model now share one baked animation file, one `Model` object, and one GPU SSBO. JSON format changed: `animations.json` now uses `packs` dict per model instead of flat `animationUrls`. Baking pipeline unchanged — one `ac-{hash}` per pack. All 46 tests passing.
- ✅ Phase 0-7 + Phase 7B + Phase 8 TALE systems fully implemented
- ✅ Phase C1 (NPC Conversation Infrastructure): Behavior, bindings, script resolution complete
- ✅ Phase C2 (Storylet-Specific Dialogue): Explicit override + tag-based fallback implemented
- ✅ Phase C3 (Mood/Tone Branches): Role-specific dialogue via npcMood(), npcWealthLabel(), npcRole() functions
- ✅ Phase C4 (Trust, Memory & Quest Hooks): Trust tracking, player memory via fact flags (readable as `props.npc.player_fact.*` in scripts), decay-based persistence (NPCs preserved as Tier-2 only if conversed-with within `TaleManager.ConversationMemorySeconds` = 30 min wall-clock; otherwise schedule dropped via `ForgetSchedule` and slot re-randomized). `IsNoticedByPlayer` retired — only conversation/`tale.npc.remember` triggers persistence. `TaleManager.IsScheduleAlive(npcId)` available for quest-code guards.
- ✅ TALE-SOCIAL Phase D1 (Scenario Pre-Computation): Chushi bakes 25 `sc-{hash}` social-structure files into `nogame/generated/`, listed in `AndroidResources.xml` / `InnoResources.iss` alongside `ac-{hash}` animations.
- ✅ TALE-SOCIAL Phase D2 (Scenario Library + Selector): `engine.tale.bake.ScenarioLibrary` is registered in `TaleModule` as a lazy singleton; `TryGet(category, index)` probes the baked file via `engine.Assets.Open` and falls through to `ScenarioCompiler.CompileInMemory()` on miss / parse error / `joyce.DisablePrebakedScenarios=true` — exact mirror of `Model.BakeAnimations` at `JoyceCode/engine/joyce/Model.cs:207-237`. `ScenarioSelector.Pick(npcCount, clusterSeed)` picks (category, index) by closest median NPC count + seeded round-robin. Both are lazy: nothing is loaded until the first `I.Get<>` request.
- ✅ TALE-SOCIAL Phase D3 (Scenario Application): `engine.tale.bake.ScenarioApplicator.Apply(scenario, realNpcs)` re-attaches a baked scenario's groups, trust edges and post-365-day property snapshot onto a freshly populated cluster. Matching is two-step: bucket both populations by role, sort each bucket by (wealth desc, morality desc, NpcId asc), then pair positionally — the rank-to-real-NpcId map carries scenario.Groups and scenario.Relationships across to the real NPCs without the scenario needing to know anything about cluster geometry. Per-NPC Trust dicts get populated directly (TaleManager has no global RelationshipTracker at runtime). Wired into `TaleManager.PopulateCluster` AFTER the warmup advance loops, so the warmup desynchronizes schedule positions and the scenario then snaps everyone into their settled social state.
- ✅ TALE-SOCIAL Phase D4 (Seedability validation): wired up `tests/JoyceCode.Tests/JoyceCode.Tests.csproj` (xUnit, references Joyce.csproj). All 46 tests pass in 168 ms — `dotnet test tests/JoyceCode.Tests/JoyceCode.Tests.csproj`. The compiler determinism test runs `CompileInMemory` twice with the same seed and asserts byte-for-byte equality — the core seedability assertion. Pre-existing dead test files under `engine/navigation/` and `engine/streets/` are excluded from compilation pending API updates / DI fixture work; csproj has explanatory comments.
- ✅ TALE-SOCIAL Phase D5 (Statistics + tuning observations): Chushi pass after the bake loop walks each `sc-{hash}` file, computes per-scenario statistics (group counts, relationship density, role distribution, per-property mean/stdev/floor/ceiling fractions), aggregates by category, and writes `nogame/generated/scenario-statistics.json` (~55 KB indented JSON). Five concrete tuning concerns surfaced — most importantly **`GroupDetector.MaxCliques = 500` is binding for the large category** (all 12 large scenarios hit exactly 500 groups, zero stdev) and **`fear` is dead across all 25 scenarios** (`mean=0.000, fractionAtFloor=1.00`). NOT auto-fixed — each is a judgment call. See `docs/tale/docs/phases/PHASE_D_SOCIAL.md` for the full design doc and the actionable tuning list.
- ✅ 200 regression tests passing (29 C-phase tests, 60-day simulations, ~5 min)
- ✅ Recalibration test framework ready (365+ days, ~2-4 hours)
- ✅ Configuration-driven roles, interactions, relationship tiers, group types
- ✅ Building role tagging with geometric attribute intensity zones (Phase 7B)
- ✅ Occupation-based character model assignment (Phase 8): roles define curated model pools in JSON config
- ✅ NavMesh street pathfinding working (Phase 7C deadlock fixed, routing Phase D fixes in place)
- ✅ Critical pathfinding bugs fixed:
  - Fallback storylet safety check (2026-03-28)
  - Same-junction pathfinding fallback using closest lanes (2026-03-28)
- ✅ PROCESS.md and documentation audit cycle in place
- 🔄 **Debug Filter Migration (Developer Infrastructure)**: Category-based selective debug output system. 307/571 (~54%) logger calls migrated across 40+ files. Implemented `DebugFilter` static class with 21 categories (Dc enum), `InterpolatedStringHandler` for zero-overhead when disabled. Per-file pattern: `private static readonly engine.Dc _dc = engine.Dc.{Category}` + `Trace(_dc, $"...")` calls. Remaining: ~264 calls across Tools, scattered utilities, testing/world/quest systems. Build verified: 0 errors.
- 🔄 Routing Phase D D2: Multi-objective A* integration pending
- 🔄 Routing Phase D D4: Behavioral variety (role-based preferences) pending
- 🔄 **TALE-SOCIAL Phase E (Living Factions)**: in progress — plan in `docs/roadmap/proposed/IMPLEMENTATION-PLAN-TALE-SOCIAL-PHASE-E.md`. Commit 1 done (2026-07-13): fixed all-groups-classify-as-"social" bake bug (Chushi/TestRunner registered an *empty* `GroupTypeRegistry`; now populated mirroring `models/nogame.group-types.json`). Commit 2 done (2026-07-13): `engine.tale.CommunityDetector` (deterministic label propagation + mutual top-K sparsification, `MaxEdgesPerNode=6`, `TrustThreshold=0.6`) replaces `GroupDetector` cliques in `DesSimulation`; baked scenarios now have 1–14 **disjoint** communities (was 56–500 overlapping cliques), membership ratio 0.74–0.80 (was 1.00); `fear` bootstrapped per role in both property generators (appended draw — existing per-seed values unchanged). Commit 3 done (2026-07-13): runtime group table + social state (E2) — `engine.tale.ClusterSocialState` (`RuntimeGroup` with type/name/roster, per-NPC `NpcSocialSnapshot`); `ScenarioApplicator.ApplyResult.Groups` carries rosters translated to real NpcIds; `TaleManager` seeds the table in `PopulateCluster`, snapshots all social state in `DepopulateCluster` and restores it on repopulate (snapshot wins over bake — social evolution survives leaving the cluster); accessors `GetGroup`/`GetSocialState`/`GetGroupmates`; deterministic faction names via `nogame.modules.tale.GroupNameGenerator` (`TaleManager.GroupNameProvider` hook); new `Dc.TaleSocial` debug category. Commit 4 done (2026-07-13): runtime social evolution tick (E3) — `engine.tale.RuntimeSocialEvolver`: every 10 game-minutes (12.5 real s) one populated cluster (round-robin) gets an encounter tick (co-located NPCs meet with the bake's probability model `P=1-(1-p_loc)^(window/15min)`, trust deltas via `InteractionTypeRegistry`, daily pair dedup, ≤12 pairs/location, homes excluded); every 2 game-hours per cluster the community structure is re-detected from the evolved trust graph and reconciled with the group table (≥50% member overlap keeps group id+name, new communities get fresh ids/names, dead groups dissolve, `Authored` groups always survive). Encounter RNG seeded from (cluster, day, tick) with a fixed integer mix — deterministic, never touches storylet RNG. Wired via `TaleModule._onSocialFrame` on `OnLogicalFrame`. Commit 5 done (2026-07-13): storylet group verbs (E4) — `"postconditions": { "group": "form" | "join" | "leave" }` extracted at parse time into `StoryletDefinition.GroupAction` (never float.Parsed); new `not_in_group` precondition; `engine.tale.GroupActions` executes verbs deterministically (form: founder + up to 4 highest-trust ungrouped neighbors ≥0.35 mutual trust, needs ≥2, group is `Authored`; join: adopt most-trusted member's group ≥0.3; leave: dissolve below 2 members) — shared by `TaleManager.AdvanceNpc` (under `_loSocial`) and `DesSimulation.ProcessNodeArrival` (sim-local table, ids from 100000, finally emits `gang_formed`). `form_gang` circularity fixed in escalation.json (`in_group` → `not_in_group` + `"group": "form"`); new `leave_gang` storylet gives factions churn. Commit 6 done (2026-07-13): player-visible factions (E5) — narration props `npc.has_group/group_type/group_name/group_size` + `npcGroupType()`/`npcGroupName()` functions; faction dialogue branches in all 5 role conversation scripts + tale.generic (drifter criminal / authority patrol_unit / merchant trade route before mood; worker/socialite social after mood); groupmate co-location via `TaleManager.ResolveSocialVenue` hangout bias (`GetGroupHangout`: modal first venue among members, cached on `RuntimeGroup.HangoutLocationId`). Commit 7 done (2026-07-13): save-game persistence (E6) — additive `GameState.TaleSocialState` (old saves compatible); `ClusterSocialState.ToJson/FromJson`; `TaleModule` save hook snapshots populated clusters first (`TaleManager.SnapshotPopulatedClusters`) then serializes all social states; load hook installs them via `ReplaceSocialState` and the PopulateCluster overlay restores onto regenerated schedules. **Phase E complete** — factions now form (bake + storylet verbs), evolve (encounter tick + re-detection), persist (snapshots + save), and are visible (faction dialogue + shared hangouts). NOTE: model JSON under models/tale/ is a runtime input to the TALE test suite AND the scenario bake — never edit while a gate runs, and re-bake (publish Chushi first) after storylet changes.
- 🔄 TALE-SOCIAL Phase D follow-up: act on the five D5 tuning concerns documented in `docs/tale/docs/phases/PHASE_D_SOCIAL.md` (MaxCliques cap, group membership ratio, relationship density, property saturation, fear=0) — being absorbed into Phase E
- ⚠️ Note: "Phase D" is overloaded — routing Phase D and TALE-SOCIAL Phase D are separate workstreams
- ⚠️ Watch for JSON deserialization issues (case-sensitive, see TaleModule.cs)

**Common First Tasks:**
- **Adding a test**: Create JSON in `models/tests/tale/phaseN-*/`, update `docs/tale/PHASE_N.md`, run `./run_tests.sh phaseN`
- **Tuning parameters**: Run `./run_recalibration_tests.sh phaseN` with `TALE_SIM_DAYS=365`
- **New phase**: Use `EnterPlanMode`, create plan in `docs/roadmap/proposed/`, follow PROCESS.md
- **Debugging**: Check `docs/tale/PHASE_N.md` for design, read actual test JSON for specs

**Key Rules (from PROCESS.md):**
- Documentation updates are MANDATORY (not optional)
- Always run `./run_tests.sh all` before commit
- Search for all references when changing systems
- Keep JSON config case-insensitive in mind (use `PropertyNameCaseInsensitive = true`)
- **Debug output pattern (mandatory)**: All debug/**trace** calls must use category-based filtering: add `private static readonly engine.Dc _dc = engine.Dc.{Category};` to class, then use `Trace(_dc, $"...")` instead of plain `Trace($"...")`. **The filtering applies to `Trace` ONLY.** `Warning(_dc, …)` and `Error(_dc, …)` prefix the category and always emit — a category decides how much *detail* to keep, never whether a problem is reported. This was not always true: until 2026-08-15 `Logger` also had a `Warning(Dc, ref DebugInterpolatedStringHandler)` overload, and since an interpolated argument prefers the handler form, **all 57 `Warning(_dc, $"...")` call sites in the tree were silently suppressed** unless that category happened to be enabled — including a `TaleSpawnOperator` health check reading *"E-to-Talk will not work"* that had never printed once. Two rounds of a live investigation treated "there are no logs" as evidence that nothing was wrong. `tests/JoyceCode.Tests/engine/LoggerFilteringTests.cs` now fails if a filtered `Warning`/`Error` overload reappears.

## Build & Run

**Prerequisites:** Check out these repos as siblings to the Karawan directory:
- `BepuPhysics2` (github.com/TimosForks/bepuphysics2)
- `DefaultEcs` (github.com/TimosForks/DefaultEcs)
- `ObjLoader` (github.com/TimosForks/ObjLoader)
- `glTF-CSharp-Loader` (github.com/KhronosGroup/glTF-CSharp-Loader) — **pin to a commit before `d8be51b`** (e.g. `git checkout d8be51b^`). That commit switched the loader from Newtonsoft.Json to System.Text.Json, and `JoyceCode/engine/{PriorityMap,rom/Loader,world/TerrainKnitter}.cs` still depend on Newtonsoft arriving transitively from it. A fresh clone of upstream `main` fails with three `CS0246: Newtonsoft` errors that name none of this.
- `ink` (github.com/TimosForks/ink)

```bash
# Build everything
dotnet build Karawan.sln

# Run desktop app
dotnet run --project Karawan/Karawan.csproj

# Run the minimal grid example
dotnet run --project examples/Launcher/Karawan.GenericLauncher.csproj
```

**Test suites** (this section used to say none existed; that has not been true for some time):

```bash
# xUnit unit/regression suite — fast, no engine boot required
dotnet test tests/JoyceCode.Tests/JoyceCode.Tests.csproj

# TALE scenario suite — needs the build-tool chain published first, see below
bash Tooling/Cmdline/build.sh && bash Chushi/build.sh
dotnet build TestRunner/TestRunner.csproj -c Release
./run_tests_parallel.sh all      # or ./run_tests.sh all
```

Three xUnit tests (`BakedAnimationLayoutTests` ×2, `BakedModelEquivalenceTests`) read
baked assets from `nogame/generated/` and fail until the asset pipeline has run at
least once — which the `TestRunner` build above does. A green run is currently
1211 xUnit + 200 TALE.

**Build notes:**
- The `nogame/generated/` directory is auto-created by an `EnsureGeneratedDirectory` MSBuild target before asset compilation. If you see build errors about missing generated files, verify this target runs first.
- Build pipeline order in `nogame.csproj`: `EnsureGeneratedDirectory` → `GatherTexturesHost` (texture packer) → `CompileAssetsHost` (Chushi) → `GatherResources` (resource compiler) → `Compile`. Chushi reads the packed atlas JSON files (`atlas-*.json`) during its texture-loading pass, so the texture packer must run first; this is enforced by `CompileAssetsHost`'s `DependsOnTargets="GatherTexturesHost"`. Without it, a fresh clone fails because Chushi has no atlas to open.

## Architecture

### ECS Foundation
The engine uses **DefaultEcs** (Entity-Component-System). Entities are composed of components; systems process entities matching component queries. Hierarchy (parent-child) is handled via Hierarchy and Transform components on entities.

### Project Structure (key projects)

| Project | Role |
|---------|------|
| **Joyce** | Core engine library: ECS, scene management, transforms, modules, physics, assets, serialization |
| **JoyceCode** (.shproj) | Engine builtins: components, systems, controllers, UI, map system, inventory, loaders (FBX/OBJ/glTF), behaviours |
| **Splash** | Abstract renderer (platform-agnostic mesh/material/texture interfaces) |
| **Splash.OpenGL** | OpenGL renderer (was `Splash.Silk`; renamed WP-5.4 — no Silk.NET left in it) |
| **Splash.API.OpenGL** | Generated OpenGL bindings from the Khronos `gl.xml` registry (was `Splash.GL`, namespace was `Karawan.Graphics.OpenGL`). Regenerate with `docs/roadmap/proposed/wp-5.1/gen.py`; never hand-edit — see KI-17 |
| **Boom** / **Boom.OpenAL** | Audio framework and OpenAL implementation |
| **BoomCode** (.shproj) | Shared audio code |
| **nogame** + **nogameCode** (.shproj) | Game-specific logic for Silicon Desert 2 |
| **Karawan** | Desktop launcher (`DesktopMain.cs`) |
| **Wuka** | Android MAUI app (packages nogame + native libs) |
| **Aihao** | Avalonia-based game editor IDE |
| **Chushi** | Asset compiler (console tool, also used as MSBuild task) |
| **Mazu** | Animation compiler |
| **Tooling/Cmdline** | CLI utilities (texture packing, resource compilation) |

Shared projects (`.shproj`) are compiled into each referencing assembly — they are not standalone DLLs.

### Configuration System (Mix)
Game configuration is JSON-based and composable. The root is `models/nogame.json` which references satellite files (`nogame.modules.json`, `nogame.implementations.json`, `nogame.resources.json`, etc.). The Mix system merges these at runtime. Key config paths:
- `/implementations` — factory/DI bindings (className + properties)
- `/modules/root/className` — main game module class
- `/mapProviders` — world map generation providers
- `/metaGen` — procedural generation operators (fragment, building, populating, cluster)
- `/scenes/catalogue` and `/scenes/startup` — scene definitions
- `/properties` — runtime-configurable values with change subscriptions
- `/quests` — quest definitions

### World Generation Pipeline
The world is built by a hierarchy of **operators**:
1. **WorldOperator** — applied to the entire world in sequence
2. **ClusterOperator** — applied to each cluster on creation
3. **FragmentOperator** — applied to each fragment on (re-)load

Everything is designed to be re-creatable on demand.

### Entity Lifecycle
Entities track a **Creator** (can serialize/deserialize) and an **Owner** (controls lifetime). Components use `[Persistable]` attribute for serialization. Save/load hooks via the Saver module (`OnBeforeSaveGame` / `OnAfterLoadGame`).

### Rendering (Splash)
Geometry is broken into **InstanceDesc** objects (mesh + materials). The renderer batches identical InstanceDescs for instanced draw calls. Platform primitives (`AMeshEntry`, `AMaterialEntry`, `ATextureEntry`) follow create → fill → upload → unload → dispose lifecycle. OpenGL version: 4.1 on macOS, 4.3 on Windows/Linux.

### Input Pipeline
Platform events → logical translation → event queue → `InputEventPipeline` (distributes by priority) → `InputController` (maps to game controller state). Higher-priority listeners consume events before the standard controller.

### Game Assembly Loading
The launcher loads game DLLs dynamically based on `game.launch.json` (`/defaults/loader/assembly`). This allows different games to run on the same engine.

### Quest System
Quests are pure ECS entities with `QuestInfo` and `Strategy` components. The old `IQuest`/`quest.Manager` system has been fully removed (Phase 5 complete). `QuestFactory` creates/activates/deactivates quest entities. Strategy-based quests use `AOneOfStrategy` for multi-phase state machines (e.g., taxi quest has pickup → driving phases). `QuestDeactivatedEvent` carries `Title` and `IsSuccess` for completion feedback. The Quest Log UI is accessible from the pause menu and supports Follow/Unfollow per quest (Phase 6+7 complete). See `QUEST_REFACTOR.md` for full migration history.

#### Followed Quest (Phase 7)
At most one active quest is the "followed" quest — only it renders its goal marker and satnav route. `SatnavService` is the central singleton managing this:
- Auto-follows the first triggered quest; auto-advances to the next when a followed quest completes
- `FollowedQuestId` persisted in `GameState` and restored on load
- Fires `QuestFollowedEvent` / `QuestUnfollowedEvent` (Code = questId)
- `ToSomewhere.OwnerQuestEntity` — set on all quest navigation targets; when set, marker and route are only created/shown while the owning quest is followed. Unset = legacy behavior (always shown).
- Quest Log UI shows Follow/Unfollow buttons per active quest (uses the newly-implemented `<if test='...'>` JT XML element)

Key classes:
- `QuestFactory` (`JoyceCode/engine/quest/QuestFactory.cs`) — quest lifecycle management (register, trigger, deactivate)
- `ISatnavService` / `SatnavService` (`JoyceCode/engine/quest/ISatnavService.cs`, `nogameCode/nogame/quest/SatnavService.cs`) — followed quest tracking, auto-follow, persistence; registered as `engine.quest.ISatnavService` in `nogame.implementations.json`
- `ToSomewhere` (`JoyceCode/engine/quest/ToSomewhere.cs`) — base module for navigation-based quest targets; set `OwnerQuestEntity` to opt into followed-quest visibility control
- `NarrationBindings` (`nogameCode/nogame/modules/story/NarrationBindings.cs`) — quest factory registrations, narration event wiring, and early `ISatnavService` initialization
- `QuestLuaBindings` (`nogameCode/nogame/quests/QuestLuaBindings.cs`) — Lua bindings: `getQuestList()` (includes `followed` field), `followQuest(id)`, `unfollowQuest()`, `isFollowed(id)`
- `ICreator` implementations — save/load quest state via `TaxiQuestData` etc.

### Placement System
`Placer` (`JoyceCode/engine/Placer.cs`) places entities in the world using `PlacementDescription` constraints:
- `MinDistance`/`MaxDistance` — horizontal distance filtering from `PlacementContext.CurrentPosition`
- `MaxAttempts` — retry loop for distance-constrained placement

### ForceSpawn API
`SpawnController.ForceSpawn(Type behaviorType, Vector3 position)` spawns a full-lifecycle character at a specific position:
- Looks up `ISpawnOperator` by behavior type
- Calls `ISpawnOperator.SpawnCharacterAt(Vector3)` (default interface method)
- Citizen implementation finds cluster/quarter/streetpoint, builds `PositionDescription`, creates entity with full Walk→Flee→Recover strategy

### Citizen Collision Routing
NPC `OnCollision` is routed through `nogame.characters.citizen.CitizenCollisionRouter` — a single static dispatcher used by `WalkBehavior`, `IdleBehavior`, `RecoverBehavior`, `TaleWalkBehavior`, `TaleConversationBehavior`. The router classifies the contact by `SolidLayerMask` and publishes one of three events on the NPC entity's event path:
- `EntityStrategy.HitEventPath` — `AnyWeapon` contact (player melee or NPC weapon) → `FleeStrategy`.
- `EntityStrategy.CrashEventPath` — `AnyVehicle` contact → `RecoverStrategy` (death animation).
- `EntityStrategy.BumpEventPath` — pure `PlayerCharacter` body contact (no weapon, no vehicle) → walking behaviors apply a transient lateral offset on `SegmentNavigator.ApplyLateralBump` (0.4 m, 400 ms linear decay, capped at 0.6 m accumulated) so the NPC steps out of the player's way without flee or collapse. Only the two walking behaviors subscribe to bump; idle/conversation/recover NPCs simply block the player.

TALE sites (`TaleWalkBehavior`, `TaleConversationBehavior`) keep their custom conversation-cancel side effect inline before calling `Dispatch`, using the router's `IsWeapon` / `IsVehicle` helpers for the classification.

### Aihao Editor IDE

Aihao is an Avalonia 11-based game editor built with **CommunityToolkit.Mvvm** and **Dock.Avalonia** for a dockable panel layout.

#### Tech Stack
- **UI**: Avalonia 11.3.8 (cross-platform desktop)
- **Layout**: Dock.Avalonia (tool windows + document tabs)
- **MVVM**: CommunityToolkit.Mvvm (`[ObservableProperty]`, `[RelayCommand]`)
- **JSON**: System.Text.Json throughout

#### JSON Loading & Storage
The editor uses **Mix** (from JoyceCode) as its single source of truth. `EditorFileProvider` (implements `IMixFileProvider`) gives Mix direct filesystem access instead of the engine's asset system. Loading flow:

1. `ProjectService.LoadProjectAsync()` creates a Mix instance with `EditorFileProvider`
2. Root JSON is loaded at `/` with priority 0; `__include__` files are discovered and tracked
3. `AihaoProject` wraps the Mix instance and exposes `GetSection(sectionId)` → `JsonNode`
4. Overlays can be added at higher priority via `AddOverlayAsync()` for debug/override configs

Saving reverses the flow: `ViewModel.ToJsonObject()` → serialize → write to disk via `ProjectService.SaveFileAsync()`.

#### Editor Architecture

Each config section (globalSettings, properties, resources, implementations, metaGen) has:
- A **DocumentViewModel** (dockable tab) that owns a section-specific editor VM
- A **section editor ViewModel** that typically wraps `JsonPropertyEditorViewModel`
- An **AXAML View** mapped via `DataTemplate` in MainWindow

The generic `JsonPropertyEditorViewModel` + `PropertyNodeViewModel` provide recursive JSON tree editing. `PropertyNodeViewModel` represents any JSON node with:
- `Name`, `Value`, `ValueKind` (String/Number/Boolean/Null/Object/Array)
- `Children` (ObservableCollection for objects/arrays)
- `IsModified` dirty tracking with callback propagation to parent
- `ToJsonNode()` / `FromJsonNode()` for round-trip serialization
- Auto-detected special editors (resolution, vector2/3, color, slider) based on key patterns and value format

#### Change Flow
```
UI TextBox → Binding → PropertyNodeViewModel.Value setter
  → Validate() → MarkModified() → _onModified callback
  → JsonPropertyEditorViewModel.IsDirty = true
  → Document tab shows dirty indicator
  → Save: ToJsonObject() → ProjectService.SaveFileAsync()
```

#### Docking Layout
- **Left pane**: Project tree (tool window)
- **Center**: Document tabs (section editors, render output)
- **Right pane**: Inspector (tool window)
- **Bottom**: Console with level filtering and search
- `AihaoDockFactory` builds the layout; `DockingService` manages registration

#### Key Services
- `ProjectService` — load/save/reload projects, overlay management
- `ProcessService` — build/run/debug game, IDE detection (Rider/VS/VS Code)
- `ActionService` — command registry with keybinding overrides
- `UserSettingsService` — persists preferences to `~/.aihao/settings.json`

#### Patterns to Follow When Adding Editors
1. Create a `FooEditorViewModel : ObservableObject` with load/save methods operating on `JsonNode`
2. Create a `FooDocumentViewModel : DocumentViewModel` wrapping the editor VM
3. Create a `FooEditor.axaml` view with bindings
4. Create a `FooDocumentView.axaml` hosting the editor view
5. Register the DataTemplate mapping in `MainWindow.axaml`
6. Register the document type in `AihaoDockFactory`
7. Add an open action in `MainWindowViewModel` + `BuiltInActions`
