{
  "name": "Interrupt with Replace scope - immediate replacement",
  "description": "NPC with interrupt priority 8+ triggers Replace interrupt, directly replacing target's arc",
  "globalTimeout": 60,
  "expected": [
    { "code": "npc_created", "count": 11 },
    { "code": "interrupt_fired", "count": { "min": 1 } },
    { "code": "node_arrival", "count": { "min": 20 } }
  ]
}
