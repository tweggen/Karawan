{
  "name": "materialization-tracking",
  "description": "Spawned NPCs are tracked as materialized, preventing double-spawn",
  "phase": "phase-6",
  "category": "materialization",
  "priority": "critical",
  "globalTimeout": 60,
  "steps": [
    {
      "expect": { "type": "tale.cluster.populated" },
      "timeout": 25,
      "comment": "Fragment has 3 NPCs"
    },
    {
      "action": "quit",
      "result": "pass",
      "comment": "After spawning 3, IsMaterialized=true for all 3, SpawnCharacter returns no-op"
    }
  ]
}
