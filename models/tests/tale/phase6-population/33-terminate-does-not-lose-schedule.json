{
  "name": "terminate-does-not-lose-schedule",
  "description": "TerminateCharacters destroys entity but NpcSchedule persists in TaleManager",
  "phase": "phase-6",
  "category": "materialization",
  "priority": "critical",
  "globalTimeout": 60,
  "steps": [
    {
      "expect": { "type": "tale.cluster.populated" },
      "timeout": 25,
      "comment": "NPC spawned and then terminated"
    },
    {
      "action": "quit",
      "result": "pass",
      "comment": "GetSchedule still returns the NPC after entity destruction"
    }
  ]
}
