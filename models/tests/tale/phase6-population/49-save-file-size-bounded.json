{
  "name": "save-file-size-bounded",
  "description": "Save file size is proportional to deviations, not total NPC count",
  "phase": "phase-6",
  "category": "persistence",
  "priority": "high",
  "globalTimeout": 30,
  "steps": [
    {
      "expect": { "type": "tale.cluster.populated" },
      "timeout": 25,
      "comment": "500 NPCs across multiple clusters, only 5 deviated"
    },
    {
      "action": "quit",
      "result": "pass",
      "comment": "TaleDeviations JSON < 5KB (5 NPCs at ~500 bytes each), not ~250KB"
    }
  ]
}
