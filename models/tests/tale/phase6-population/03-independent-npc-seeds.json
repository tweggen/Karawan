{
  "name": "independent-npc-seeds",
  "description": "Each NPC seed is independent — skipping index N does not affect index N+1",
  "phase": "phase-6",
  "category": "population-generator",
  "priority": "critical",
  "globalTimeout": 30,
  "steps": [
    {
      "expect": { "type": "world.cluster.completed" },
      "timeout": 20,
      "comment": "Cluster completes and population generated"
    },
    {
      "expect": { "type": "tale.cluster.populated" },
      "timeout": 10,
      "comment": "Generate with skip mask {3}, verify NPC at index 4 is identical to full generation"
    },
    {
      "action": "quit",
      "result": "pass",
      "comment": "Seed independence validated: string-concat pattern not sequential RNG"
    }
  ]
}
