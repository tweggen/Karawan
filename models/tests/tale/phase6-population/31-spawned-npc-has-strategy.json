{
  "name": "spawned-npc-has-strategy",
  "description": "Materialized NPC has TaleEntityStrategy attached",
  "phase": "phase-6",
  "category": "materialization",
  "priority": "critical",
  "globalTimeout": 60,
  "steps": [
    {
      "expect": { "type": "world.cluster.completed" },
      "timeout": 20,
      "comment": "Cluster ready"
    },
    {
      "expect": { "type": "tale.cluster.populated" },
      "timeout": 10,
      "comment": "NPCs registered"
    },
    {
      "action": "quit",
      "result": "pass",
      "comment": "Entity has TaleEntityStrategy that references the correct NpcSchedule"
    }
  ]
}
