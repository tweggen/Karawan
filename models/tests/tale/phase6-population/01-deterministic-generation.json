{
  "name": "deterministic-generation",
  "description": "Same cluster seed always produces identical NPC population",
  "phase": "phase-6",
  "category": "population-generator",
  "priority": "critical",
  "globalTimeout": 30,
  "steps": [
    {
      "expect": { "type": "world.cluster.completed" },
      "timeout": 20,
      "comment": "Wait for first cluster to complete"
    },
    {
      "expect": { "type": "tale.cluster.populated" },
      "timeout": 10,
      "comment": "TaleModule populates cluster with NPC schedules"
    },
    {
      "action": "quit",
      "result": "pass",
      "comment": "Cluster population is deterministic from seed — same seed always produces same NPCs"
    }
  ]
}
