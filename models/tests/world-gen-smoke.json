{
  "name": "world-gen-smoke",
  "description": "Verify world generation pipeline: clusters generated, streets built, cluster operators run",
  "globalTimeout": 180,
  "steps": [
    {
      "expect": { "type": "world.cluster.completed" },
      "timeout": 120,
      "comment": "At least one cluster must complete street generation + cluster operators (includes engine boot time)"
    },
    {
      "action": "quit",
      "result": "pass"
    }
  ]
}
